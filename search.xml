<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>CSS面试</title>
    <url>/20200303CSS%E9%9D%A2%E8%AF%95/</url>
    <content><![CDATA[<h2 id="css盒模型"><a href="#css盒模型" class="headerlink" title="css盒模型"></a>css盒模型</h2><blockquote>
<p>css盒模型基本概念？</p>
</blockquote>
<ol>
<li><p>盒模型由content（内容区域），padding（内边距），border（边框），margin（外边距）组成。</p>
</li>
<li><p>当对一个文档进行布局的时候，浏览器的渲染引擎会根据这两个标准的其中一个将所有元素表示为一个个矩形的盒子</p>
</li>
<li><p>分类：标准模型、IE模型</p>
</li>
</ol>
<blockquote>
<p>标准模型和IE模型的区别：计算高度和宽度的不同，怎么不同，高度宽度是怎么计算的？</p>
</blockquote>
<ol>
<li>标准模型</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">div宽度 &#x3D; content &#x2F;&#x2F;改变padding或border宽度，div宽度会变化</span><br><span class="line">box-sizing:content-box;  &#x2F;*设置标准盒子*&#x2F;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>IE模型（怪异盒子模型）</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">div宽度 &#x3D; content + padding + border &#x2F;&#x2F;改变padding或border宽度，div宽度不会变化</span><br><span class="line">box-sizing: border-box;  &#x2F;*设置标准盒子*&#x2F;</span><br></pre></td></tr></table></figure>

<p><img src="/20200303CSS%E9%9D%A2%E8%AF%95/%E7%9B%92%E5%AD%90%E6%A8%A1%E5%9E%8B.png" alt="盒子模型"></p>
]]></content>
  </entry>
  <entry>
    <title>Gulp学习</title>
    <url>/Gulp%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<p>构建工具:<br>  自动化。对于需要反复重复的任务，例如压缩（minification）、编译、单元测试、linting等，自动化工具可以减轻你的劳动，简化你的工作。<br>  gulp或者grunt都仅仅是一个操作平台，他们本身做不了任何事情，要做事情需要通过插件</p>
<p>1.gulp的使用<br>  全局安装：<br>      npm install –global gulp<br>  创建项目：<br>      LearnGulp<br>  项目依赖安装：<br>      npm install –save-dev gulp<br>  在项目根目录下创建一个名为 gulpfile.js 的文件：<br>      var gulp = require(‘gulp’);<br>      gulp.task(‘default’, function() {<br>      // 将你的默认的任务代码放在这<br>      });<br>  运行<br>      gulp<br>2.gulp的方法<br>  gulp.task(str,fn)<br>    创建一个gulp任务<br>  gulp.src(path)<br>    文件来源<br>  gulp.dest(path)<br>    操作之后的文件到哪里去<br>  .pipe(package)<br>    执行一个gulp功能<br>  gulp.watch()<br>    监听<br>  gulp.start()<br>    执行gulp任务<br>3.插件<br>  1.压缩JavaScript文件<br>    1.安装插件<br>      npm install –save-dev gulp-uglify<br>      代码<br>      gulp.task(“jsuglify”,function(){<br>        gulp.src(“src/js/demo.js”)<br>            .pipe(jsUglify())<br>            .pipe(gulp.dest(“dist/js”))<br>      })<br>  2.压缩CSS文件<br>    1.安装<br>      npm install –save-dev gulp-minify-css<br>  3.压缩HTML文件<br>    1.安装<br>      npm install –save-dev gulp-minify-html<br>  4.图片压缩<br>    npm install –save-dev gulp-imagemin<br>  5.代码检查<br>    npm install –save-dev gulp-jshint jshint<br>    公司learder自己编写代码规范，按照他的规范来写代码！！！<br>  6.合并、重命名<br>    npm install –save-dev gulp-concat gulp-rename<br>  7.Less编译为CSS文件<br>    npm install –save-dev gulp-less<br>  8.监听<br>    gulp.task(“watchLess”,function(){<br>      gulp.watch(“src/css/*.less”,function(){<br>        gulp.run(“reless”)<br>      })<br>    })<br>  9.热更新：<br>    1.命令：npm install gulp-livereload –save-dev<br>    2.全局服务器：npm install -g http-server<br>    3.浏览器打开：chrome://extensions/ 浏览器插件：LiveReload （直接点击启动）<br>    4.编写热更新的代码<br>    5.启动热更新<br>      1.在项目根目录下启动http-server<br>      2.启动热更新：hot<br>      3.打开浏览器启动项目<br>      4.启动浏览器（livereload）插件，将空心圆点成实心圆</p>
]]></content>
      <tags>
        <tag>Gulp</tag>
      </tags>
  </entry>
  <entry>
    <title>8月记录</title>
    <url>/21-8%E6%9C%88%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h2 id="08-02"><a href="#08-02" class="headerlink" title="08-02"></a>08-02</h2><h3 id="react-router实现嵌套路由url变化后局部组件变化"><a href="#react-router实现嵌套路由url变化后局部组件变化" class="headerlink" title="react-router实现嵌套路由url变化后局部组件变化"></a>react-router实现嵌套路由url变化后局部组件变化</h3><ul>
<li><p>children方式</p>
</li>
<li><p>标签方式</p>
</li>
</ul>
<p>ant-design-pro中通过children实现</p>
<h3 id="react-router配置中一个页面不需要BasicLayout一个需要"><a href="#react-router配置中一个页面不需要BasicLayout一个需要" class="headerlink" title="react-router配置中一个页面不需要BasicLayout一个需要"></a>react-router配置中一个页面不需要BasicLayout一个需要</h3><p>可以如下配置</p>
<p>其中：有component的不能写<code>redirect</code>会白屏</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">routes: [</span><br><span class="line">    &#123;</span><br><span class="line">      path: &#39;&#x2F;&#39;,</span><br><span class="line">      component: &#39;..&#x2F;layouts&#x2F;SecurityLayout&#39;,</span><br><span class="line">      routes: [</span><br><span class="line">        &#123;</span><br><span class="line">          path: &#39;&#x2F;&#39;,</span><br><span class="line">          redirect: &#39;&#x2F;home&#x2F;projects&#39;</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">          path: &#39;&#x2F;report&#x2F;:reportId&#39;,  &#x2F;&#x2F; 详情页</span><br><span class="line">          component: &#39;.&#x2F;Report&#x2F;index&#39;,</span><br><span class="line">          routes: [</span><br><span class="line">            &#123;</span><br><span class="line">              path: &#39;&#x2F;report&#x2F;:reportId&#39;,</span><br><span class="line">              redirect: &#39;&#x2F;report&#x2F;:reportId&#x2F;summaryHome&#39; &#x2F;&#x2F; 初始化路由</span><br><span class="line">            &#125;,</span><br><span class="line">            &#123;</span><br><span class="line">              path: &#39;&#x2F;report&#x2F;:reportId&#x2F;summaryHome&#39;,</span><br><span class="line">              component: &#39;.&#x2F;Report&#x2F;SummaryHome&#39;,</span><br><span class="line">            &#125;,</span><br><span class="line">            &#123;</span><br><span class="line">              path: &#39;&#x2F;report&#x2F;:reportId&#x2F;cpuUsage&#39;,</span><br><span class="line">              component: &#39;.&#x2F;Report&#x2F;Cpu&#x2F;cpuUsage&#39;,</span><br><span class="line">            &#125;</span><br><span class="line">          ]</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">          path: &#39;&#x2F;home&#39;,</span><br><span class="line">          component: &#39;..&#x2F;layouts&#x2F;BasicLayout&#39;,</span><br><span class="line">          Routes: [&#39;src&#x2F;pages&#x2F;Authorized&#39;],</span><br><span class="line">          authority: [&#39;admin&#39;, &#39;manageMeasurement&#39;, &#39;editMeasurement&#39;, &#39;readMeasurement&#39;],</span><br><span class="line">          routes: [</span><br><span class="line">            &#123;</span><br><span class="line">              path: &#39;&#x2F;home&#x2F;projects&#39;, &#x2F;&#x2F; 外层项目页</span><br><span class="line">              component: &#39;.&#x2F;Summary&#x2F;index&#39;,</span><br><span class="line">            &#125;</span><br><span class="line">          ]</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">          path: &#39;&#x2F;404&#39;,</span><br><span class="line">          component: &#39;.&#x2F;404&#39;,</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">          path: &#39;&#x2F;403&#39;,</span><br><span class="line">          component: &#39;.&#x2F;403&#39;,</span><br><span class="line">        &#125;,</span><br><span class="line">      ],</span><br><span class="line">    &#125;,</span><br><span class="line">  ],</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="08-05"><a href="#08-05" class="headerlink" title="08-05"></a>08-05</h2><h3 id="antd-多个tab，checkbox中value绑定问题"><a href="#antd-多个tab，checkbox中value绑定问题" class="headerlink" title="antd 多个tab，checkbox中value绑定问题"></a>antd 多个tab，checkbox中value绑定问题</h3><p>有以下场景：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">tab1   tab2   tab3</span><br><span class="line">checkbox1   checkbox2</span><br></pre></td></tr></table></figure>

<p>在ant design中切换tab2后<code>onChange</code>的内容只有当前tab的</p>
<p>因此，如果checkbox的<code>defaultValue</code>解决不了需求只能用<code>value</code>绑定的话，可以这样解决：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">onCheckBoxChange(checkedValues) &#123;</span><br><span class="line">    const &#123; checkBoxData, tabSelectKey &#125; &#x3D; this.state</span><br><span class="line">    if (tabSelectKey) &#123;</span><br><span class="line">      if (!checkBoxData[tabSelectKey]) checkBoxData[tabSelectKey] &#x3D; []</span><br><span class="line">      checkBoxData[tabSelectKey] &#x3D; checkedValues</span><br><span class="line">      this.setState(&#123;</span><br><span class="line">        checkBoxData: checkBoxData</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;</span><br><span class="line">    let cbDefaultVal &#x3D; []</span><br><span class="line">    Object.values(checkBoxData).map((item) &#x3D;&gt; &#123;</span><br><span class="line">      cbDefaultVal &#x3D; [...cbDefaultVal, ...item]</span><br><span class="line">    &#125;)</span><br><span class="line">    this.setState(&#123;</span><br><span class="line">      cbDefaultVal</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>按照tab的值分类分别存储checkbox的值，value使用的时候把对象中的数组合并一下获取所有的值。</p>
<h3 id="项目使用ant-design库后无样式"><a href="#项目使用ant-design库后无样式" class="headerlink" title="项目使用ant design库后无样式"></a>项目使用ant design库后无样式</h3><ol>
<li>检查是否引入css</li>
</ol>
<p>在入口css中加入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@import &#39;~antd&#x2F;dist&#x2F;antd.css&#39;;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li><p>是否没安装<code>babel-plugin-import</code></p>
</li>
<li><p>检查webpack打包是否加入了<code>css module</code></p>
</li>
</ol>
<p>css module是webpack打包时会在css的类前面加上对应的模块名，使用了cssmodule的可以像下面这种情况使用类</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import styles from &#39;.&#x2F;index.css&#39;</span><br><span class="line"></span><br><span class="line">function Test() &#123;</span><br><span class="line">  return (</span><br><span class="line">    &lt;div className&#x3D;&#123;styles.class1&#125;&gt;Content&lt;&#x2F;div&gt;</span><br><span class="line">  )</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">export default Test</span><br></pre></td></tr></table></figure>

<p>原因：可能是因为打包时把ant design的css也加上对应的模块名了，因此样式失效</p>
<p>待解决：看是否需要加入其它插件让css module和ant兼容</p>
<p>以下的webpack未尝试<br><a href="https://blog.csdn.net/XinYe666666/article/details/100585642" target="_blank" rel="noopener">react引入antd样式失效</a></p>
<h3 id="怎么获取路由的地址冒号后的参数"><a href="#怎么获取路由的地址冒号后的参数" class="headerlink" title="怎么获取路由的地址冒号后的参数"></a>怎么获取路由的地址冒号后的参数</h3><p>react-router中配置了/:project的在页面里怎么获取project这个参数</p>
<p>this.props里面有match的属性，但是里面获取到的是跳转之前的端口后面的值</p>
<p>在当前页面的this.props.match.params.id里面是可以找到参数的</p>
<p>在props里有<code>location</code>和<code>match</code>参数</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const &#123; location, match &#125; &#x3D; this.props  &#x2F;&#x2F; 在构造函数 constructor(props) 中获取时 直接是 props</span><br><span class="line">location.pathname  &#x2F;&#x2F; 获取当前路由（完整），如 &#x2F;hello&#x2F;world_1</span><br><span class="line">match.path  &#x2F;&#x2F; 父级路由，如 &#x2F;hello</span><br></pre></td></tr></table></figure>

<h3 id="react父组件调用子组件方法"><a href="#react父组件调用子组件方法" class="headerlink" title="react父组件调用子组件方法"></a>react父组件调用子组件方法</h3><ol>
<li>通过Ref的方式</li>
</ol>
<p>父组件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bindRef (ref) &#123;</span><br><span class="line">    this.child &#x3D; ref</span><br><span class="line">  &#125;</span><br><span class="line">  clickMethod &#x3D; () &#x3D;&gt; &#123;</span><br><span class="line">    this.child.childMethod()</span><br><span class="line">  &#125;</span><br><span class="line">  render () &#123;</span><br><span class="line">    return (</span><br><span class="line">      &lt;div className&#x3D;&#39;wrapper&#39;&gt;</span><br><span class="line">        &lt;p onClick&#x3D;&#123;this.clickMethod&#125;&gt;这是一个click事件&lt;&#x2F;p&gt;</span><br><span class="line">        &lt;child onRef&#x3D;&#123;this.bindRef.bind(this)&#125;&gt;&lt;&#x2F;child&gt;</span><br><span class="line">      &lt;&#x2F;div&gt;</span><br><span class="line">    )</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>子组件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">componentDidMount () &#123;</span><br><span class="line">  this.props.onRef(this)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">childMethod () &#123;</span><br><span class="line">  console.log(&#39;我是子组件的方法&#39;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Ant-Design-Pro-中-proxy-配置了但是提示404"><a href="#Ant-Design-Pro-中-proxy-配置了但是提示404" class="headerlink" title="Ant Design Pro 中 proxy 配置了但是提示404"></a>Ant Design Pro 中 proxy 配置了但是提示404</h3><p>配置pathRewrite</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">proxy: &#123;</span><br><span class="line">  &#39;&#x2F;api&#x2F;&#39;: &#123;</span><br><span class="line">    target: &#39;http:&#x2F;&#x2F;xxx:3000&#39;,</span><br><span class="line">    changeOrigin: true,</span><br><span class="line">    pathRewrite: &#123;&#39;^&#x2F;api&#39;: &#39;&#39;&#125;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="css怎么获取第一个之外的下一个元素"><a href="#css怎么获取第一个之外的下一个元素" class="headerlink" title="css怎么获取第一个之外的下一个元素"></a>css怎么获取第一个之外的下一个元素</h3><p>ant-design的checkbox中有以下结构</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div&gt;</span><br><span class="line">  &lt;span&gt;&lt;input &#x2F;&gt;&lt;&#x2F;span&gt;</span><br><span class="line">  &lt;span&gt;名称1&lt;&#x2F;span&gt;</span><br><span class="line">&lt;&#x2F;div&gt;</span><br></pre></td></tr></table></figure>

<p>现在是想让名称1的文字部分超过宽度后变成缩略点的形式</p>
<p>解决：使用<code>+</code>选择符</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;style&gt;</span><br><span class="line">.dom div+div&#123;</span><br><span class="line">   background:red;</span><br><span class="line">&#125;</span><br><span class="line">&lt;&#x2F;style&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li>选择器：如果需要选择紧接在另一个元素后的元素，而且二者有相同的父元素，可以使用相邻兄弟选择器。</li>
</ul>
<p>或者 <code>~</code>选择符</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;style&gt;</span><br><span class="line">.dom div~div&#123;</span><br><span class="line">  background:red;</span><br><span class="line">&#125;</span><br><span class="line">&lt;&#x2F;style&gt;</span><br></pre></td></tr></table></figure>

<p>~ 选择器 ：作用是查找某一个指定元素的后面的所有兄弟结点。<br>由于都是div元素，第一个元素没有兄弟元素，所以就能获取除第一个之外的子元素。</p>
]]></content>
      <tags>
        <tag>月份记录</tag>
      </tags>
  </entry>
  <entry>
    <title>JS设计模式 - 面向对象</title>
    <url>/JS%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h2 id="类和对象的概念"><a href="#类和对象的概念" class="headerlink" title="类和对象的概念"></a>类和对象的概念</h2><blockquote>
<p>类就是拥有相等功能和相同的属性的对象的集合 – 抽象的<br>对象就是类的实例 – 具体</p>
</blockquote>
<p>生活角度看类和对象</p>
<p>如果人是类，小名就是对象、小花也是对象<br>如果电脑是类，桌上的这两台电脑就是对象</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Person &#123;</span><br><span class="line">  constructor(name, age) &#123;</span><br><span class="line">    this.name &#x3D; name;</span><br><span class="line">    this.age &#x3D; age;</span><br><span class="line">  &#125;</span><br><span class="line">  say() &#123;</span><br><span class="line">    alert(&#96;$&#123;this.name&#125; is eating&#96;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 调用</span><br><span class="line">var wang &#x3D; new Person(&quot;wang&quot;, 20); &#x2F;&#x2F; 初始化小王</span><br><span class="line">wang.say();  &#x2F;&#x2F; wang is eating</span><br><span class="line">var zhang &#x3D; new Person(&quot;zhang&quot;, 50); &#x2F;&#x2F; 初始化小张</span><br><span class="line">zhang.say(); &#x2F;&#x2F; zhang is eating</span><br></pre></td></tr></table></figure>

<h2 id="面向对象三要素：封装，继承，多态"><a href="#面向对象三要素：封装，继承，多态" class="headerlink" title="面向对象三要素：封装，继承，多态"></a>面向对象三要素：封装，继承，多态</h2><h3 id="继承（子类继承父类）"><a href="#继承（子类继承父类）" class="headerlink" title="继承（子类继承父类）"></a>继承（子类继承父类）</h3><blockquote>
<p>作用：继承可将公共方法抽离出来，提高复用，减少冗余</p>
</blockquote>
<p>Person类与上面的类一样</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Student extends Person &#123;</span><br><span class="line">  constructor(name, age, number) &#123;</span><br><span class="line">    super(name, age);</span><br><span class="line">    this.number &#x3D; number;</span><br><span class="line">  &#125;</span><br><span class="line">  study() &#123;</span><br><span class="line">    alert(&#96;$&#123;this.name&#125; is studing&#96;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">var xiaoming &#x3D; new Student(&#39;xiaoming&#39;, 10, &#39;A1&#39;);</span><br><span class="line">xiaoming.say();   &#x2F;&#x2F; xiaoming is eating</span><br><span class="line">xiaoming.study(); &#x2F;&#x2F; xiaoming is studing</span><br></pre></td></tr></table></figure>

<h3 id="封装"><a href="#封装" class="headerlink" title="封装"></a>封装</h3><blockquote>
<p>目前ts内可用，比较规范 以下代码需ts环境下运行 <a href="http://www.typescriptlang.org/play/" target="_blank" rel="noopener">http://www.typescriptlang.org/play/</a></p>
</blockquote>
<ul>
<li><ol>
<li>public 完全开放</li>
</ol>
</li>
<li><ol start="2">
<li>protected 对子类开放</li>
</ol>
</li>
<li><ol start="3">
<li>private 对自己开放</li>
</ol>
</li>
</ul>
<p>作用：</p>
<ul>
<li><ol>
<li>减少耦合，不该外露的不外露</li>
</ol>
</li>
<li><ol start="2">
<li>利于数据，接口的权限管理</li>
</ol>
</li>
<li><ol start="3">
<li>ES6暂时不支持，一般认为 _开头的属性是private</li>
</ol>
</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Person &#123;</span><br><span class="line">  name: any</span><br><span class="line">  age: number</span><br><span class="line">  protected weight: any &#x2F;&#x2F; 受保护的属性，只有自己或者子类可以访问</span><br><span class="line">  constructor(name: any, age: number) &#123;</span><br><span class="line">    this.name &#x3D; name;</span><br><span class="line">    this.age &#x3D; age;</span><br><span class="line">    this.weight &#x3D; 120</span><br><span class="line">  &#125;</span><br><span class="line">  say() &#123;</span><br><span class="line">    alert(&#96;$&#123;this.name&#125; is eating&#96;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class Student extends Person &#123;</span><br><span class="line">  number: number</span><br><span class="line">  private girlfriend: any</span><br><span class="line">  constructor(name: any, age: number, number: number) &#123;</span><br><span class="line">    super(name, age);</span><br><span class="line">    this.number &#x3D; number;</span><br><span class="line">    this.girlfriend &#x3D; &#39;xiaoli&#39;</span><br><span class="line">  &#125;</span><br><span class="line">  study() &#123;</span><br><span class="line">    alert(&#96;$&#123;this.name&#125; is studing&#96;);</span><br><span class="line">  &#125;</span><br><span class="line">  getWeight() &#123;</span><br><span class="line">    alert(&#96;weight $&#123;this.weight&#125;&#96;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">let xiaoming &#x3D; new Student(&#39;xiaoming&#39;, 10, 12)</span><br><span class="line">xiaoming.getWeight()</span><br></pre></td></tr></table></figure>

<h3 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h3><blockquote>
<p>同一个接口，不同的实现<br>JS 应用极少<br>需要结合Java等语言的接口，重写，重载等功能</p>
</blockquote>
<p>作用：</p>
<ul>
<li>保持了子类的开放性和灵活性</li>
<li>面向接口编程(JS引用较少)</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class A extends Person &#123;</span><br><span class="line">  constructor(name) &#123;</span><br><span class="line">    super(name)</span><br><span class="line">  &#125;</span><br><span class="line">  saySomething() &#123;</span><br><span class="line">    alert(&quot;Im A&quot;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class B extends Person &#123;</span><br><span class="line">  constructor(name) &#123;</span><br><span class="line">    super(name)</span><br><span class="line">  &#125;</span><br><span class="line">  saySomething() &#123;</span><br><span class="line">    alert(&quot;Im B&quot;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">let a &#x3D; new A(&#39;A&#39;)</span><br><span class="line">a.saySomething() &#x2F;&#x2F; Im A</span><br><span class="line">let b &#x3D; new B(&#39;B&#39;)</span><br><span class="line">b.saySomething() &#x2F;&#x2F; Im B</span><br></pre></td></tr></table></figure>

<h2 id="JS应用面向对象实例："><a href="#JS应用面向对象实例：" class="headerlink" title="JS应用面向对象实例："></a>JS应用面向对象实例：</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;**</span><br><span class="line"> * -----------------------------------------------------------------</span><br><span class="line"> * JS应用实例</span><br><span class="line"> * JQuery</span><br><span class="line"> *&#x2F;</span><br><span class="line"></span><br><span class="line">class JQuery &#123;</span><br><span class="line">  constructor(selector) &#123;</span><br><span class="line">    let slice &#x3D; Array.prototype.slice</span><br><span class="line">    let dom &#x3D; slice.call(document.querySelectorAll(selector))</span><br><span class="line">    let len &#x3D; dom ? dom.length : 0</span><br><span class="line">    for (let i &#x3D; 0; i &lt; len; i++) &#123;</span><br><span class="line">      this[i] &#x3D; dom[i]</span><br><span class="line">    &#125;</span><br><span class="line">    this.length &#x3D; len</span><br><span class="line">    this.selector &#x3D; selector</span><br><span class="line">  &#125;</span><br><span class="line">  append(node) &#123;</span><br><span class="line">    &#x2F;&#x2F; ...</span><br><span class="line">  &#125;</span><br><span class="line">  addClass(name) &#123;</span><br><span class="line">    &#x2F;&#x2F; ...</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">window.$ &#x3D; function(selector) &#123;</span><br><span class="line">  &#x2F;&#x2F; 工厂模式</span><br><span class="line">  return new JQuery(selector)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="为什么要使用面向对象"><a href="#为什么要使用面向对象" class="headerlink" title="为什么要使用面向对象"></a>为什么要使用面向对象</h2><blockquote>
<p>程序执行：顺序，判断，循环  —结构化</p>
</blockquote>
<blockquote>
<p>面向对象 — 数据结构化</p>
</blockquote>
<p>对于计算机，结构化的东西才是最简单的</p>
<p>编程应该 简单 &amp; 抽象</p>
<h2 id="使用面向对象意义"><a href="#使用面向对象意义" class="headerlink" title="使用面向对象意义"></a>使用面向对象意义</h2><p><strong>数据结构化</strong></p>
]]></content>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>React Hooks系列useEffect</title>
    <url>/ReactHooks%E7%B3%BB%E5%88%97-useEffect/</url>
    <content><![CDATA[<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><p>在使用React Hooks时，React Hooks作用非常大，该系列归纳工作中遇到的关于React Hooks api的应用场景和查找资料中深入了解的一些问题，对React的掌握更上一层楼，该系列包含以下内容：</p>
<ul>
<li><a href="/ReactHooks系列-useEffect" target="_blank">useEffect</a></li>
</ul>
<p>以下为useEffect的使用：</p>
<h2 id="前提了解"><a href="#前提了解" class="headerlink" title="前提了解"></a>前提了解</h2><p>想了解UseEffect先深入了解Function Component的渲染机制，Function Component 与 Class Component 功能上的不同具体可看<a href="https://zhuanlan.zhihu.com/p/59558396" target="_blank">精读《Function VS Class 组件》</a>文章了解</p>
<p><strong>Function Component 是更彻底的状态驱动抽象，甚至没有 Class Component 生命周期的概念，只有一个状态，而 React 负责同步到 DOM。</strong>请先忘掉React的生命周期，重新理解一下 Function Component 的思维方式。</p>
<p>那问题来了？Function Component没有生命周期，hooks是怎么实现数据的变化和相关生命周期的？</p>
<p>原因：</p>
<p>React Hooks 拥有 Capture Value 特性</p>
<p>每次 Render 的内容都会形成一个快照并保留下来，因此当状态变更而 reRender 时，就形成了 N 个 Render 状态，而每个 Render 状态都拥有自己固定不变的 Props 与 State</p>
<p>本文涉及重点：<strong>Capture Value</strong></p>
<h2 id="从几个疑问开始"><a href="#从几个疑问开始" class="headerlink" title="从几个疑问开始"></a>从几个疑问开始</h2><ul>
<li>🤔 useEffect是什么时候开始调用的？</li>
<li>🤔 如何用 useEffect 代替 componentDidMount?</li>
<li>🤔 如何用 useEffect 取数？参数 [] 代表什么？</li>
<li>🤔useEffect 的依赖可以是函数吗？是哪些函数？</li>
<li>🤔 为何有时候取数会触发死循环？</li>
<li>🤔 为什么有时候在 useEffect 中拿到的 state 或 props 是旧的？</li>
</ul>
<h3 id="Q1-useEffect是什么时候开始调用的？"><a href="#Q1-useEffect是什么时候开始调用的？" class="headerlink" title="Q1: useEffect是什么时候开始调用的？"></a>Q1: useEffect是什么时候开始调用的？</h3><p>useEffect是在页面dom元素渲染后调用</p>
<h3 id="Q2-如何用-useEffect-代替-componentDidMount"><a href="#Q2-如何用-useEffect-代替-componentDidMount" class="headerlink" title="Q2: 如何用 useEffect 代替 componentDidMount?"></a>Q2: 如何用 useEffect 代替 componentDidMount?</h3><h2 id="参考资源"><a href="#参考资源" class="headerlink" title="参考资源"></a>参考资源</h2><p><a href="https://juejin.cn/post/6844903806090608647#heading-16" target="_blank">精读《useEffect 完全指南》</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/59558396" target="_blank">精读《Function VS Class 组件》</a></p>
]]></content>
      <tags>
        <tag>React</tag>
        <tag>useEffect</tag>
      </tags>
  </entry>
  <entry>
    <title>ReactHooks系列-useRef</title>
    <url>/ReactHooks%E7%B3%BB%E5%88%97-useRef/</url>
    <content><![CDATA[<h2 id="useRef"><a href="#useRef" class="headerlink" title="useRef"></a>useRef</h2><p>使用场景</p>
<ul>
<li>获取上一轮的props和state</li>
</ul>
<p>为什么useRef和useEffect配合可以获取上一轮的props和state？</p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://blog.csdn.net/bu_leng/article/details/116485236" target="_blank" rel="noopener">react函数组件如何获取上一轮的props和state? 我用hooks这样来获取</a></p>
]]></content>
  </entry>
  <entry>
    <title>css特殊性</title>
    <url>/css%E7%89%B9%E6%AE%8A%E6%80%A7/</url>
    <content><![CDATA[<p>一下被问到愣住了，现在查阅了资料发现是经常用的，还是要积累一下</p>
<h2 id="CSS的特殊性"><a href="#CSS的特殊性" class="headerlink" title="CSS的特殊性"></a>CSS的特殊性</h2><p>特殊性是用于计算元素最终使用哪个样式。</p>
<p>先看以下等级划分(由高到低)：</p>
<ol>
<li>important，!important          <strong>等级为无穷大∞</strong></li>
<li>内联样式，在标签内style=””。     <strong>等级为1000</strong></li>
<li>ID选择器，#content。            <strong>等级为100</strong></li>
<li>类选择器，.content。            <strong>等级为10</strong></li>
<li>标签选择器，伪元素选择器，div div:before <strong>等级为1</strong></li>
<li>通配符，*                       <strong>等级为0</strong></li>
<li>继承                            <strong>不贡献，比0还小</strong></li>
</ol>
<h2 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h2><p>继承不能继承边框，盒子模型的外边距、内边距等等。<br>继承是不贡献特殊性的，但要注意和0的区别。<br>0特殊性会优先于无特殊性</p>
<p>如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">*&#123;color: gray&#125;</span><br><span class="line">h1&#123;color: black&#125;</span><br><span class="line"> </span><br><span class="line">&lt;h1&gt;css &lt;em&gt;specificity&lt;&#x2F;em&gt;&lt;&#x2F;h1&gt;</span><br></pre></td></tr></table></figure>

<p>其中css为black色，em标签里的specificity为gray色，因为0特殊性比继承值优先级高</p>
<h2 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h2><p>如果遇到了似乎没有起作用的CSS规则，很可能是出现了特殊性冲突。请在你的选择器中添加他的一个父元素的ID，从而提高它的特殊性。如果这能解决问题，就说明样式表中其他地方很可能有更特殊的规则，它覆盖了你的规则。如果是这种情况，你可能需要检查代码，解决特殊性冲突，让代码尽可能简洁。</p>
]]></content>
      <tags>
        <tag>css</tag>
        <tag>前端面试</tag>
      </tags>
  </entry>
  <entry>
    <title>如何用Hexo和Git搭建一个免费的线上博客</title>
    <url>/buildHexoBlog/</url>
    <content><![CDATA[<h1 id="什么是Hexo？"><a href="#什么是Hexo？" class="headerlink" title="什么是Hexo？"></a>什么是Hexo？</h1><p>Hexo是一个快速、简洁且高效的博客框架。基于node.js；支持MarkDown语法；可扩展性强；博客主题众多，。</p>
<h1 id="开始搭建"><a href="#开始搭建" class="headerlink" title="开始搭建"></a>开始搭建</h1><h2 id="1-安装Hexo"><a href="#1-安装Hexo" class="headerlink" title="1. 安装Hexo"></a>1. 安装Hexo</h2><p>所需环境：</p>
<ul>
<li>node</li>
<li>git </li>
</ul>
<p>利用npm安装Hexo</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ npm install -g hexo-cli hexo</span><br></pre></td></tr></table></figure>

<h2 id="2-本地搭建Hexo"><a href="#2-本地搭建Hexo" class="headerlink" title="2. 本地搭建Hexo"></a>2. 本地搭建Hexo</h2><ol>
<li>安装 Hexo 完成后，进入电脑的文件夹内打开控制台(shift + 右键 -&gt; 在此处打开powershell窗口)</li>
<li>执行下列命令，Hexo 将会在指定文件夹中新建所需要的文件：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo init &lt;folder&gt;</span><br><span class="line">$ <span class="built_in">cd</span> &lt;folder&gt;</span><br><span class="line">$ npm install</span><br></pre></td></tr></table></figure>

<p>新建完成后文件夹目录如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">├── _config.yml</span><br><span class="line">├── package.json</span><br><span class="line">├── scaffolds</span><br><span class="line">├── source</span><br><span class="line">|   ├── _drafts</span><br><span class="line">|   └── _posts</span><br><span class="line">└── themes</span><br></pre></td></tr></table></figure>

<p><a href="https://hexo.io/zh-cn/docs/index.html" target="_blank" rel="noopener" title="hexo官网">最新操作查看官方文档</a></p>
<h2 id="3-下载主题（如果想用原装主题的可跳过）"><a href="#3-下载主题（如果想用原装主题的可跳过）" class="headerlink" title="3. 下载主题（如果想用原装主题的可跳过）"></a>3. 下载主题（如果想用原装主题的可跳过）</h2><p>主题可在官网的theme目录下进入：</p>
<p><img src="/buildHexoBlog/theme1.png" alt="主题"></p>
<p>我选用的主题是叫<strong>hexo-theme-snippet</strong>的</p>
<p>github地址为<code>https://github.com/shenliyang/hexo-theme-snippet</code></p>
<p>样子是这样的（主要看重右侧的站内搜索功能）：</p>
<p><img src="/buildHexoBlog/theme2.png" alt="主题2"></p>
<p>具体安装步骤可见github上<a href="https://github.com/shenliyang/hexo-theme-snippet" target="_blank" rel="noopener" title="主题文档">文档</a>，但有需要注意的地方：</p>
<p><img src="/buildHexoBlog/gulp.png" alt="gulp"></p>
<p>执行这一步，会出现以下的问题：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Cannot find module &#39;gulp-less&#39;</span><br></pre></td></tr></table></figure>

<p>解决：</p>
<p>导致这个问题是因为执行这个gulp时引用的包没有安装</p>
<p>我们可以打开themes/hexo-theme-snippet/gulpfile.js</p>
<p><img src="/buildHexoBlog/gulp2.png" alt="gulp2"></p>
<p>最好的解决方法，在package.json加上这段</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;dependencies&quot;: &#123;</span><br><span class="line">  &quot;gulp-autoprefixer&quot;: &quot;^7.0.1&quot;,</span><br><span class="line">  &quot;gulp-htmlclean&quot;: &quot;^2.7.22&quot;,</span><br><span class="line">  &quot;gulp-htmlmin&quot;: &quot;^5.0.1&quot;,</span><br><span class="line">  &quot;gulp-less&quot;: &quot;^4.0.1&quot;,</span><br><span class="line">  &quot;gulp-minify-css&quot;: &quot;^1.2.4&quot;,</span><br><span class="line">  &quot;gulp-notify&quot;: &quot;^3.2.0&quot;,</span><br><span class="line">  &quot;gulp-plumber&quot;: &quot;^1.2.1&quot;,</span><br><span class="line">  &quot;gulp-rename&quot;: &quot;^2.0.0&quot;,</span><br><span class="line">  &quot;gulp-rev-append&quot;: &quot;^0.1.8&quot;,</span><br><span class="line">  &quot;gulp-sequence&quot;: &quot;^1.0.0&quot;,</span><br><span class="line">  &quot;gulp-uglify&quot;: &quot;^3.0.2&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后再 <code>npm install</code> 一次</p>
<p>安装完包后还会遇到一个问题：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">default function must be specified</span><br></pre></td></tr></table></figure>

<p>造成这个问题的原因是gulp版本的问题，因为这个主题是用的gulp3，而现在是gulp4的版本，不支持原来的写法，解决方法如下：</p>
<p>原来：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 同步执行task</span><br><span class="line">gulp.task(&#39;deploy&#39;, sequence([&#39;minify-css&#39;, &#39;minify-js&#39;], &#39;rev&#39;, &#39;minify-html&#39;));</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 部署前代码处理</span><br><span class="line">gulp.task(&#39;default&#39;, [&#39;deploy&#39;], function(e) &#123;</span><br><span class="line">    console.log(&quot;[complete] please execute： hexo d&quot;);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>更改为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 同步执行task</span><br><span class="line">gulp.task(&#39;deploy&#39;, gulp.series(gulp.parallel([&#39;minify-css&#39;, &#39;minify-js&#39;]), &#39;rev&#39;, &#39;minify-html&#39;));</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 部署前代码处理</span><br><span class="line">gulp.task(&#39;default&#39;, gulp.parallel([&#39;deploy&#39;]), function(e) &#123;</span><br><span class="line">    console.log(&quot;[complete] please execute： hexo d&quot;);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>其中</p>
<ul>
<li>gulp.parallel: 可以并行计算</li>
<li>gulp.series: 按照顺序执行</li>
</ul>
<p>在_config.yml里设置主题</p>
<p>找到theme选项并设置</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">theme: hexo-theme-snippet</span><br></pre></td></tr></table></figure>

<p>现在在控制台输入<code>hexo g</code>打开本地项目就能看到运用了主题后的博客了！！</p>
<h2 id="4-在外网上挂载项目"><a href="#4-在外网上挂载项目" class="headerlink" title="4. 在外网上挂载项目"></a>4. 在外网上挂载项目</h2><p>我们使用Git自带的一个功能，而且还免费~</p>
<p>首先创建一个Git项目,项目名为自己的用户名加.github.io,比如我的项目名叫：<code>wenzhenyi.github.io</code></p>
<p><img src="/buildHexoBlog/git2.png" alt="git2"></p>
<p>打开存放hexo项目的文件夹，找到_config.yml配置文件，找到deploy，进行如下配置，完成保存。</p>
<p><img src="/buildHexoBlog/set1.png" alt="set1"></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repository: https:&#x2F;&#x2F;github.com&#x2F;wenzhenyi&#x2F;wenzhenyi.github.io.git   &#x2F;&#x2F;将地址换成自己的</span><br><span class="line">  branch: master</span><br></pre></td></tr></table></figure>

<p>仓库地址可以在这里复制：</p>
<p><img src="/buildHexoBlog/git1.png" alt="git1"></p>
<p>在控制台执行以下命令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">运行：hexo clean  &#x2F;&#x2F; 清除缓存文件 (db.json) 和已生成的静态文件 (public)</span><br><span class="line">运行：hexo g  &#x2F;&#x2F; 本地生成静态文件</span><br><span class="line">运行：hexo d  &#x2F;&#x2F; 将本地静态文件推送至Github</span><br></pre></td></tr></table></figure>

<p><strong>注意：推送到github其实是生成的静态文件</strong></p>
<p>最后访问：<code>http://wenzhenyi.github.io</code></p>
<hr>
<h1 id="记录遇到的BUG"><a href="#记录遇到的BUG" class="headerlink" title="记录遇到的BUG"></a>记录遇到的BUG</h1><h2 id="系统上禁止运行脚本："><a href="#系统上禁止运行脚本：" class="headerlink" title="系统上禁止运行脚本："></a>系统上禁止运行脚本：</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo : 无法加载文件 C:\Users\Administrator\AppData\Roaming\npm\hexo.ps1，因为在此系统上禁止运行脚本。有关详细信息，请参阅 http:&#x2F;&#x2F;go.micr</span><br><span class="line">osoft.com&#x2F;fwlink&#x2F;?LinkID&#x3D;135170 中的 about_Execution_Policies。</span><br><span class="line">所在位置 行:1 字符: 1</span><br></pre></td></tr></table></figure>

<p>问题解决：</p>
<ol>
<li>以管理员身份运行powershell</li>
<li>执行<code>set-executionpolicy remotesigned</code></li>
<li>输入 <code>y</code></li>
</ol>
<h2 id="SyntaxError-Invalid-regular-expression-flags"><a href="#SyntaxError-Invalid-regular-expression-flags" class="headerlink" title="SyntaxError: Invalid regular expression flags"></a>SyntaxError: Invalid regular expression flags</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">return data.replace(&#x2F;&lt;head&gt;(?!&lt;\&#x2F;head&gt;).+?&lt;\&#x2F;head&gt;&#x2F;s, str &#x3D;&gt; str.replace(&#39;&lt;&#x2F;head&gt;&#39;, &#96;$&#123;hexoGeneratorTag&#125;&lt;&#x2F;head&gt;&#96;));</span><br></pre></td></tr></table></figure>

<p>问题解决:</p>
<ol>
<li>升级node版本(原本是8.4的)</li>
</ol>
]]></content>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>css等高的方式</title>
    <url>/css%E7%AD%89%E9%AB%98%E7%9A%84%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<p><strong>问题：在一列里，div之间自适应等高，内容垂直居中</strong></p>
<p>html：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div class&#x3D;&quot;wrap&quot;&gt;</span><br><span class="line">  &lt;div class&#x3D;&quot;item&quot;&gt;AAAA&lt;&#x2F;div&gt;</span><br><span class="line">  &lt;div class&#x3D;&quot;item&quot;&gt;BBBBBBBBBBBBBBBBBB&lt;&#x2F;div&gt;</span><br><span class="line">  &lt;div class&#x3D;&quot;item&quot;&gt;A&lt;&#x2F;div&gt;</span><br><span class="line">  &lt;div class&#x3D;&quot;item&quot;&gt;CCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCC&lt;&#x2F;div&gt;</span><br><span class="line">  &lt;div class&#x3D;&quot;item&quot;&gt;A&lt;&#x2F;div&gt;</span><br><span class="line">&lt;&#x2F;div&gt;</span><br></pre></td></tr></table></figure>

<p>css：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.item &#123;</span><br><span class="line">  width: 10%;</span><br><span class="line">  border: 1px solid red;</span><br><span class="line">  word-break: break-all;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="1-使用css-Flex布局"><a href="#1-使用css-Flex布局" class="headerlink" title="1. 使用css Flex布局"></a>1. 使用css Flex布局</h2><p><a href="http://www.ruanyifeng.com/blog/2015/07/flex-grammar.html" target="_blank" rel="noopener" title="flex布局">flex布局文档</a></p>
<p>父div设置<code>display: flex</code>时，子div可等高</p>
<p>css：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.wrap&#123;</span><br><span class="line">  display: flex; &#x2F;&#x2F; 等高</span><br><span class="line">&#125;</span><br><span class="line">.item &#123;</span><br><span class="line">  display: flex;  &#x2F;&#x2F; 内容垂直居中</span><br><span class="line">  align-items:center; &#x2F;&#x2F; 内容垂直居中</span><br><span class="line">  justify-content:center; &#x2F;&#x2F; 内容垂直居中</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>效果如下图所示：</p>
<p><img src="/css%E7%AD%89%E9%AB%98%E7%9A%84%E6%96%B9%E5%BC%8F/css1.png" alt="css1"></p>
<hr>
<h2 id="2-使用css-Table布局"><a href="#2-使用css-Table布局" class="headerlink" title="2. 使用css Table布局"></a>2. 使用css Table布局</h2><p>css：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.wrap&#123;</span><br><span class="line">  display: table;</span><br><span class="line">&#125;</span><br><span class="line">.item &#123;</span><br><span class="line">  display: table-cell;</span><br><span class="line">  text-align: center;</span><br><span class="line">  vertical-align: middle;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>效果跟上面一样</p>
<p>优点：支持ie8/ie9</p>
<hr>
<h2 id="3-使用CSS-网格Grid"><a href="#3-使用CSS-网格Grid" class="headerlink" title="3. 使用CSS 网格Grid"></a>3. 使用CSS 网格Grid</h2><p><a href="http://www.ruanyifeng.com/blog/2019/03/grid-layout-tutorial.html" target="_blank" rel="noopener" title="Grid布局">Grid布局文档</a></p>
<p>css：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.wrap&#123;</span><br><span class="line">  display: grid;</span><br><span class="line">  grid-gap: 10px;</span><br><span class="line">  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));</span><br><span class="line">&#125;</span><br><span class="line">.item &#123;</span><br><span class="line">  width: 100%;</span><br><span class="line">  word-break: break-all;</span><br><span class="line">  border: 1px solid red;</span><br><span class="line">  margin: auto 0;</span><br><span class="line">  text-align: center;</span><br><span class="line">  align-self: center;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>暂未实现通过grid实现内容垂直居中，效果如下：</p>
<p><img src="/css%E7%AD%89%E9%AB%98%E7%9A%84%E6%96%B9%E5%BC%8F/css2.png" alt="css2"></p>
<p>缺点：兼容性问题差，ie不兼容，个人感觉难上手</p>
<h2 id="4-使用CSS-padding-bottom-margin-bottom正负值相抵-等高"><a href="#4-使用CSS-padding-bottom-margin-bottom正负值相抵-等高" class="headerlink" title="4. 使用CSS padding-bottom|margin-bottom正负值相抵 (等高)"></a>4. 使用CSS padding-bottom|margin-bottom正负值相抵 (等高)</h2><p>首先把列的padding-bottom设为一个足够大的值，再把列的margin-bottom设一个与前面的padding- bottom的正值相抵消的负值，父容器设置超出隐藏，这样子父容器的高度就还是它里面的列没有设定padding-bottom时的高度，当它里面的任 一列高度增加了，则父容器的高度被撑到它里面最高那列的高度，其他比这列矮的列则会用它们的padding-bottom来补偿这部分高度差。因为背景是 可以用在padding占用的空间里的，而且边框也是跟随padding变化的，所以就成功的完成了一个障眼法。</p>
<p>css:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.wrap&#123;</span><br><span class="line">  width: 52%;</span><br><span class="line">  overflow: hidden;</span><br><span class="line">&#125;</span><br><span class="line">.item &#123;</span><br><span class="line">  width: 10%;</span><br><span class="line">  display: inline-block;</span><br><span class="line">  vertical-align: top;</span><br><span class="line">  word-break: break-all;</span><br><span class="line">  border: 1px solid red;</span><br><span class="line">  padding-bottom:2000px;</span><br><span class="line">  margin-bottom:-2000px;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>效果图如下：</p>
<p><img src="/css%E7%AD%89%E9%AB%98%E7%9A%84%E6%96%B9%E5%BC%8F/css3.png" alt="css3"></p>
<p>优点：兼容性最好<br>缺点：下边框会不显示，需改用父元素的border-bottom，垂直居中难实现</p>
<hr>
<p>参考：</p>
<p><a href="https://www.cnblogs.com/chenhuichao/p/8682925.html" target="_blank" rel="noopener" title="关于垂直居中的方法总结！">关于垂直居中的方法总结！</a></p>
]]></content>
      <tags>
        <tag>css</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>vsCode中常用的插件</title>
    <url>/vsCode%E4%B8%AD%E5%B8%B8%E7%94%A8%E7%9A%84%E6%8F%92%E4%BB%B6/</url>
    <content><![CDATA[<h2 id="1-AutoFileName"><a href="#1-AutoFileName" class="headerlink" title="1. AutoFileName"></a>1. AutoFileName</h2><p>自动补充文件名</p>
<h2 id="2-Chinese"><a href="#2-Chinese" class="headerlink" title="2. Chinese"></a>2. Chinese</h2><p>适用于 VS Code 的中文（简体）语言包</p>
<h2 id="3-Code-Runner"><a href="#3-Code-Runner" class="headerlink" title="3. Code Runner"></a>3. Code Runner</h2><p>用于跑小小一段的代码</p>
<h2 id="4-Git-History-Diff"><a href="#4-Git-History-Diff" class="headerlink" title="4. Git History Diff"></a>4. Git History Diff</h2><p>查看git的历史记录以及比较</p>
<h2 id="5-JavaScript-ES6-code-snippets"><a href="#5-JavaScript-ES6-code-snippets" class="headerlink" title="5. JavaScript(ES6) code snippets"></a>5. JavaScript(ES6) code snippets</h2><p>自动补充es6语法</p>
<h2 id="6-Live-Server"><a href="#6-Live-Server" class="headerlink" title="6. Live Server"></a>6. Live Server</h2><p>在当前文件夹创建小型服务器</p>
<h2 id="7-Open-in-Browser"><a href="#7-Open-in-Browser" class="headerlink" title="7. Open in Browser"></a>7. Open in Browser</h2><p>右键从浏览器打开此文件</p>
<h2 id="8-Prettier-Code-formatter"><a href="#8-Prettier-Code-formatter" class="headerlink" title="8. Prettier - Code formatter"></a>8. Prettier - Code formatter</h2><p>格式化代码</p>
<h2 id="9-Vetur"><a href="#9-Vetur" class="headerlink" title="9. Vetur"></a>9. Vetur</h2><p>vue集成的工具</p>
<h2 id="10-VS-Color-Picker"><a href="#10-VS-Color-Picker" class="headerlink" title="10. VS Color Picker"></a>10. VS Color Picker</h2><p>颜色选择器</p>
]]></content>
      <tags>
        <tag>vscode</tag>
        <tag>2020年3月</tag>
      </tags>
  </entry>
  <entry>
    <title>工具网站</title>
    <url>/%E5%B7%A5%E5%85%B7%E7%BD%91%E7%AB%99/</url>
    <content><![CDATA[<h2 id="BootCDN-获取线上常用插件的地址"><a href="#BootCDN-获取线上常用插件的地址" class="headerlink" title="BootCDN-获取线上常用插件的地址"></a>BootCDN-获取线上常用插件的地址</h2><p>稳定、快速、免费的前端开源项目 CDN 加速服务</p>
<p><a href="https://www.bootcdn.cn/" target="_blank" rel="noopener" title="BootCDN">BootCDN</a></p>
<h2 id="MDN-前端技术文档"><a href="#MDN-前端技术文档" class="headerlink" title="MDN-前端技术文档"></a>MDN-前端技术文档</h2><p><a href="https://developer.mozilla.org/zh-CN/" target="_blank" rel="noopener" title="MDN">前端技术文档MDN</a></p>
<h2 id="Can-I-Use网站-查找各属性方法在各浏览器的兼容情况"><a href="#Can-I-Use网站-查找各属性方法在各浏览器的兼容情况" class="headerlink" title="Can I Use网站-查找各属性方法在各浏览器的兼容情况"></a>Can I Use网站-查找各属性方法在各浏览器的兼容情况</h2><p><a href="https://www.caniuse.com/" target="_blank" rel="noopener" title="CanIUse">Can I Use</a></p>
<h2 id="Autoprefixer-CSS-online-在线添加浏览器css兼容情况"><a href="#Autoprefixer-CSS-online-在线添加浏览器css兼容情况" class="headerlink" title="Autoprefixer CSS online-在线添加浏览器css兼容情况"></a>Autoprefixer CSS online-在线添加浏览器css兼容情况</h2><p><a href="http://autoprefixer.github.io/" target="_blank" rel="noopener" title="Autoprefixer">Autoprefixer</a></p>
<h2 id="淘宝-NPM-镜像"><a href="#淘宝-NPM-镜像" class="headerlink" title="淘宝 NPM 镜像"></a>淘宝 NPM 镜像</h2><p><a href="https://developer.aliyun.com/mirror/NPM?from=tnpm" target="_blank" rel="noopener" title="NPM">NPM镜像</a></p>
<h2 id="前端好用的开发工具"><a href="#前端好用的开发工具" class="headerlink" title="前端好用的开发工具"></a>前端好用的开发工具</h2><p>FeHelper: chrome插件，包含JSON接口自动格式化，代码美化，代码压缩，二维码生成器，页面取色工具等工具</p>
<p><a href="https://github.com/zxlie/FeHelper/tree/master/apps/static/screenshot/crx" target="_blank" rel="noopener" title="FeHelper">FeHelper</a></p>
]]></content>
  </entry>
  <entry>
    <title>手写一个Promise</title>
    <url>/%E6%89%8B%E5%86%99%E4%B8%80%E4%B8%AAPromise/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>promise的使用场景很多，手写一个promise也是对自己js知识的一个巩固</p>
]]></content>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>网络协议从入门到底层原理 -- 基本概念</title>
    <url>/%E7%BD%91%E7%BB%9C%E5%8D%8F%E8%AE%AE%E8%AF%BE%E7%A8%8B1/</url>
    <content><![CDATA[<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>Q：数据如何从一个设备传递到另外一个设备？</p>
<p>通过网络协议规定</p>
<p><img src="/%E7%BD%91%E7%BB%9C%E5%8D%8F%E8%AE%AE%E8%AF%BE%E7%A8%8B1/01-1.png" alt="常见网络协议面试题"></p>
<p>C/C++跨平台原理：</p>
<p>使用平台相应的编译器生成对应平台的可执行格式文件</p>
<p>Java跨平台原理：</p>
<p>了解：</p>
<p>JVM（Java Virtual Machine）：Java虚拟机</p>
<p>Java的跨平台：一次编译，到处运行</p>
<ul>
<li>每个平台安装JVM（软件）。</li>
<li>编译生成跟平台无关的字节码文件（class文件）</li>
<li>由相应平台的JVM解析字节码为机器指令（010101）</li>
<li>代码有错则编译失败</li>
</ul>
<p>JDK,JRE,JVM关系</p>
<p>JDK包含JRE（java runtime environment）<br>JRE包含JVM</p>
<h2 id="网络互联模型"><a href="#网络互联模型" class="headerlink" title="网络互联模型"></a>网络互联模型</h2><p><img src="/%E7%BD%91%E7%BB%9C%E5%8D%8F%E8%AE%AE%E8%AF%BE%E7%A8%8B1/01-2.png" alt="网络互联模型"></p>
<p>简单来说：</p>
<p>OSI: 七层模型是偏理论的</p>
<p>TCP/IP: 四层模型是实际应用中实践出来的，被广泛认可的</p>
<p>五层模型是偏向学习使用的</p>
<h2 id="计算机之间通信的基础："><a href="#计算机之间通信的基础：" class="headerlink" title="计算机之间通信的基础："></a>计算机之间通信的基础：</h2><ul>
<li><p>首先需要知道<strong>网卡地址（Mac/网卡硬件）地址</strong></p>
</li>
<li><p>其次是需要<strong>IP地址</strong></p>
</li>
</ul>
<p>计算机之间需要通信一开始是不知道双方的网卡地址的，因此利用广播发送<strong>ARP</strong>协议，获取机器的Mac地址，然后把Mac地址缓存在本地。</p>
<p><code>Ping</code>命令实际是发送<strong>ICMP</strong>协议的数据包，包含双方IP地址和双方Mac地址</p>
<h2 id="计算机之间的连接方式-–-网线直连"><a href="#计算机之间的连接方式-–-网线直连" class="headerlink" title="计算机之间的连接方式 – 网线直连"></a>计算机之间的连接方式 – 网线直连</h2><p>网卡地址</p>
<p>Ping（ICMP协议）</p>
<p>APR协议</p>
<p>目的：获取Mac地址</p>
<p>优势：有缓存</p>
<h2 id="计算机之间的连接方式-–-同轴电缆（没有智商）"><a href="#计算机之间的连接方式-–-同轴电缆（没有智商）" class="headerlink" title="计算机之间的连接方式 – 同轴电缆（没有智商）"></a>计算机之间的连接方式 – 同轴电缆（没有智商）</h2><h2 id="计算机之间的连接方式-–-集线器（没有智商）"><a href="#计算机之间的连接方式-–-集线器（没有智商）" class="headerlink" title="计算机之间的连接方式 – 集线器（没有智商）"></a>计算机之间的连接方式 – 集线器（没有智商）</h2><p>hub：计算器</p>
<h2 id="计算机之间的连接方式-–-网桥"><a href="#计算机之间的连接方式-–-网桥" class="headerlink" title="计算机之间的连接方式 – 网桥"></a>计算机之间的连接方式 – 网桥</h2><p>优点：</p>
<p>隔绝冲突域（有智商）</p>
<p>缺点：</p>
<p>同一边的还是会冲突</p>
<h2 id="计算机之间的连接方式-–-交换机"><a href="#计算机之间的连接方式-–-交换机" class="headerlink" title="计算机之间的连接方式 – 交换机"></a>计算机之间的连接方式 – 交换机</h2><p>相当于接口更多的网桥</p>
<p>全双工通信</p>
<p>交换机 – 局域网的最终方案</p>
<p>比集线器安全</p>
<p>缺点：</p>
<p>ARP广播，同一个网段的都能接受到</p>
<h2 id="计算机之间的连接方式-–-路由器"><a href="#计算机之间的连接方式-–-路由器" class="headerlink" title="计算机之间的连接方式 – 路由器"></a>计算机之间的连接方式 – 路由器</h2>]]></content>
  </entry>
  <entry>
    <title>静下来好好学习的网站</title>
    <url>/%E9%9D%99%E4%B8%8B%E6%9D%A5%E5%A5%BD%E5%A5%BD%E5%AD%A6%E4%B9%A0%E7%9A%84%E7%BD%91%E7%AB%99/</url>
    <content><![CDATA[<h2 id="scrimba"><a href="#scrimba" class="headerlink" title="scrimba"></a>scrimba</h2><p>全英文学习</p>
<p><a href="https://scrimba.com/" target="_blank" rel="noopener" title="scrimba">scrimba</a></p>
]]></content>
  </entry>
  <entry>
    <title>2021-11月记录</title>
    <url>/21-11%E6%9C%88%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h2 id="微信小程序扫码接口，wx-scanCode-OBJECT"><a href="#微信小程序扫码接口，wx-scanCode-OBJECT" class="headerlink" title="微信小程序扫码接口，wx.scanCode(OBJECT)"></a>微信小程序扫码接口，wx.scanCode(OBJECT)</h2><p><a href="https://blog.csdn.net/weixin_34122548/article/details/92504534" target="_blank" rel="noopener">https://blog.csdn.net/weixin_34122548/article/details/92504534</a></p>
<h2 id="nodejs读取图片并将二进制数据转换成base64格式"><a href="#nodejs读取图片并将二进制数据转换成base64格式" class="headerlink" title="nodejs读取图片并将二进制数据转换成base64格式"></a>nodejs读取图片并将二进制数据转换成base64格式</h2><p><code>const buffer = new Buffer(data, &#39;binary&#39;).toString(&#39;base64&#39;);</code></p>
<h2 id="MYSQL两个group-by的select语句怎么组合一起"><a href="#MYSQL两个group-by的select语句怎么组合一起" class="headerlink" title="MYSQL两个group by的select语句怎么组合一起"></a>MYSQL两个group by的select语句怎么组合一起</h2><p><a href="https://www.nuomiphp.com/eplan/620604.html" target="_blank" rel="noopener">https://www.nuomiphp.com/eplan/620604.html</a></p>
<h2 id="ROW-NUMBER"><a href="#ROW-NUMBER" class="headerlink" title="ROW_NUMBER()"></a>ROW_NUMBER()</h2><h2 id="qr-image的使用"><a href="#qr-image的使用" class="headerlink" title="qr-image的使用"></a>qr-image的使用</h2><p><a href="https://www.npmjs.com/package/node-qr-image" target="_blank" rel="noopener">https://www.npmjs.com/package/node-qr-image</a></p>
<h2 id="js中实现根据某个字段进行排序"><a href="#js中实现根据某个字段进行排序" class="headerlink" title="js中实现根据某个字段进行排序"></a>js中实现根据某个字段进行排序</h2><p>sort方法</p>
<h2 id="小程序订阅消息？-“errcode”-44002-”errmsg”-”empty-post-data"><a href="#小程序订阅消息？-“errcode”-44002-”errmsg”-”empty-post-data" class="headerlink" title="小程序订阅消息？{“errcode”:44002,”errmsg”:”empty post data}"></a>小程序订阅消息？{“errcode”:44002,”errmsg”:”empty post data}</h2><h2 id="微信小程序码获取-从频繁失败到成功率100"><a href="#微信小程序码获取-从频繁失败到成功率100" class="headerlink" title="微信小程序码获取-从频繁失败到成功率100%"></a>微信小程序码获取-从频繁失败到成功率100%</h2><p><a href="https://developers.weixin.qq.com/community/develop/article/doc/0000cc805683281d77a86435656013" target="_blank" rel="noopener">https://developers.weixin.qq.com/community/develop/article/doc/0000cc805683281d77a86435656013</a></p>
<a id="more"></a>

<h2 id="java-vue-element-小程序码后端生成和前端展示"><a href="#java-vue-element-小程序码后端生成和前端展示" class="headerlink" title="java+vue element 小程序码后端生成和前端展示"></a>java+vue element 小程序码后端生成和前端展示</h2><p><a href="https://blog.csdn.net/qq_38977441/article/details/106396113" target="_blank" rel="noopener">https://blog.csdn.net/qq_38977441/article/details/106396113</a></p>
<h2 id="微信获取小程序码createQRCode-get-getUnlimited区别"><a href="#微信获取小程序码createQRCode-get-getUnlimited区别" class="headerlink" title="微信获取小程序码createQRCode,get,getUnlimited区别"></a>微信获取小程序码createQRCode,get,getUnlimited区别</h2><h2 id="详解微信小程序带参数二维码的生成以及参数的获取"><a href="#详解微信小程序带参数二维码的生成以及参数的获取" class="headerlink" title="详解微信小程序带参数二维码的生成以及参数的获取"></a>详解微信小程序带参数二维码的生成以及参数的获取</h2><p><a href="http://doofuu.com/article/4156146.html" target="_blank" rel="noopener">http://doofuu.com/article/4156146.html</a></p>
<h2 id="midway中间件的使用"><a href="#midway中间件的使用" class="headerlink" title="midway中间件的使用"></a>midway中间件的使用</h2><p><a href="http://www.midwayjs.org/docs/web_middleware" target="_blank" rel="noopener">http://www.midwayjs.org/docs/web_middleware</a></p>
<h2 id="微信小程序获取jscode2sessiot提示ninvalid-code"><a href="#微信小程序获取jscode2sessiot提示ninvalid-code" class="headerlink" title="微信小程序获取jscode2sessiot提示ninvalid code"></a>微信小程序获取jscode2sessiot提示ninvalid code</h2><p>微信小程序出现“invalid code”的解决方法：我们只需要在开发者工具的详情界面中修改Appid即可。小程序分发的code是与appid绑定的，仅当前申请code的appid才可使用。</p>
<h2 id="小程序已认证getPhoneNumber-fail-no-permission"><a href="#小程序已认证getPhoneNumber-fail-no-permission" class="headerlink" title="小程序已认证getPhoneNumber:fail no permission"></a>小程序已认证getPhoneNumber:fail no permission</h2><h2 id="uni-app怎么返回上一页"><a href="#uni-app怎么返回上一页" class="headerlink" title="uni-app怎么返回上一页"></a>uni-app怎么返回上一页</h2><p><a href="https://zhuanlan.zhihu.com/p/346593866" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/346593866</a></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">back() &#123;</span><br><span class="line">  &#x2F;*uni.navigateBack(&#123;</span><br><span class="line">    delta: 1</span><br><span class="line">  &#125;)*&#x2F;</span><br><span class="line">  &#x2F;&#x2F;@zxyuns 处理兼容，如果没有上一级界面则返回首页</span><br><span class="line">  const pages &#x3D; getCurrentPages();</span><br><span class="line">  if (pages.length &#x3D;&#x3D;&#x3D; 2) &#123;</span><br><span class="line">    uni.navigateBack(&#123;</span><br><span class="line">      delta: 1</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125; else if (pages.length &#x3D;&#x3D;&#x3D; 1) &#123;</span><br><span class="line">    uni.switchTab(&#123;</span><br><span class="line">      url: &#39;&#x2F;pages&#x2F;home&#x2F;home&#39;,</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    uni.navigateBack(&#123;</span><br><span class="line">      delta: 1</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<h2 id="uni-app-页面-amp-组件生命周期"><a href="#uni-app-页面-amp-组件生命周期" class="headerlink" title="uni-app 页面&amp;组件生命周期"></a>uni-app 页面&amp;组件生命周期</h2><p><a href="https://www.jianshu.com/p/880c600a8be7" target="_blank" rel="noopener">https://www.jianshu.com/p/880c600a8be7</a></p>
<h2 id="nodejs利用string-random生成指定的随机字符串"><a href="#nodejs利用string-random生成指定的随机字符串" class="headerlink" title="nodejs利用string-random生成指定的随机字符串"></a>nodejs利用string-random生成指定的随机字符串</h2><p><a href="https://www.cnblogs.com/songqingbo/articles/10951569.html" target="_blank" rel="noopener">https://www.cnblogs.com/songqingbo/articles/10951569.html</a></p>
<h2 id="在同一个sql语句中，统计不同条件的Count数量"><a href="#在同一个sql语句中，统计不同条件的Count数量" class="headerlink" title="在同一个sql语句中，统计不同条件的Count数量"></a>在同一个sql语句中，统计不同条件的Count数量</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">方案一</span><br><span class="line"></span><br><span class="line">select id,</span><br><span class="line">count(case when 属性 in (1,2) then 1 else 0 end) as sum1,</span><br><span class="line">count(case when  属性 in (3) then 1 else 0 end) as sum2,</span><br><span class="line">sum(case when  属性 in (4,5) then 1 else 0 end) as sum3</span><br><span class="line">from 表名</span><br><span class="line">group by id</span><br><span class="line"> </span><br><span class="line">方案二</span><br><span class="line"> </span><br><span class="line"> SELECT   </span><br><span class="line">count( * ) AS num,  </span><br><span class="line">count(if(字段&#x3D;1,true,null)) AS sum1,   </span><br><span class="line"></span><br><span class="line">count(if字段&#x3D;2,true,null)) AS sum2   </span><br><span class="line"></span><br><span class="line">FROM 表名 </span><br><span class="line"></span><br><span class="line">WHERE  其他条件</span><br></pre></td></tr></table></figure>

<p><a href="https://www.cnblogs.com/chenliangcl/p/7283574.html" target="_blank" rel="noopener">https://www.cnblogs.com/chenliangcl/p/7283574.html</a></p>
<h2 id="微信小程序主要的点"><a href="#微信小程序主要的点" class="headerlink" title="微信小程序主要的点"></a>微信小程序主要的点</h2><p>微信登录：</p>
<p>一、 先熟悉好流程</p>
<p>使用wx.login()获取用户的openid和session_key</p>
<p>二、使用<code>getUserInfoProfiler</code></p>
<p>微信支付：</p>
<h2 id="MySQL数据库表结构设计：小程序商城-用户表"><a href="#MySQL数据库表结构设计：小程序商城-用户表" class="headerlink" title="MySQL数据库表结构设计：小程序商城-用户表"></a>MySQL数据库表结构设计：小程序商城-用户表</h2><p><a href="http://open.yesapi.cn/tablelist/yesapi_eb_user.html" target="_blank" rel="noopener">http://open.yesapi.cn/tablelist/yesapi_eb_user.html</a></p>
<h2 id="Mysql用户·角色·权限·表的设计"><a href="#Mysql用户·角色·权限·表的设计" class="headerlink" title="Mysql用户·角色·权限·表的设计"></a>Mysql用户·角色·权限·表的设计</h2><ol>
<li>通过字符串的形式</li>
</ol>
<p><a href="https://www.cnblogs.com/myseries/p/10871633.html" target="_blank" rel="noopener">RBAC</a></p>
<h2 id="关键字group-by-、-Having的-用法"><a href="#关键字group-by-、-Having的-用法" class="headerlink" title="关键字group by 、 Having的 用法"></a>关键字group by 、 Having的 用法</h2><p>GROUP BY我们可以先从字面上来理解，GROUP表示分组，BY后面写字段名，就表示根据哪个字段进行分组，如果有用Excel比较多的话，GROUP BY比较类似Excel里面的透视表。<br>GROUP BY必须得配合聚合函数来用，分组之后你可以计数（COUNT），求和（SUM），求平均数（AVG）等。</p>
<p>HAVING<br>当然提到GROUP BY 我们就不得不提到HAVING，HAVING相当于条件筛选，但它与WHERE筛选不同，HAVING是对于GROUP BY对象进行筛选。</p>
<h2 id="sql去重；同一条数据出现多条取一条的sql语句"><a href="#sql去重；同一条数据出现多条取一条的sql语句" class="headerlink" title="sql去重；同一条数据出现多条取一条的sql语句"></a>sql去重；同一条数据出现多条取一条的sql语句</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-- count函数</span><br><span class="line">select count(姓名)</span><br><span class="line">from student;</span><br><span class="line"> </span><br><span class="line">select count(distinct 姓名) &#x2F;*去除重复姓名*&#x2F;</span><br><span class="line">from student;</span><br></pre></td></tr></table></figure>

<p><a href="https://blog.csdn.net/weixin_39840733/article/details/111099703" target="_blank" rel="noopener">https://blog.csdn.net/weixin_39840733/article/details/111099703</a></p>
<p><a href="https://www.cnblogs.com/mark5/p/11115750.html" target="_blank" rel="noopener">https://www.cnblogs.com/mark5/p/11115750.html</a></p>
<h2 id="JavaScript中将字符串true或false转换成Boolean类型"><a href="#JavaScript中将字符串true或false转换成Boolean类型" class="headerlink" title="JavaScript中将字符串true或false转换成Boolean类型"></a>JavaScript中将字符串true或false转换成Boolean类型</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var isTrueSet &#x3D; ((myValue || &#39;&#39;).toLowerCase() &#x3D;&#x3D;&#x3D; &#39;true&#39;);</span><br></pre></td></tr></table></figure>

<p>直接对比字符串</p>
<h2 id="uni-app-生命周期函数"><a href="#uni-app-生命周期函数" class="headerlink" title="uni-app 生命周期函数"></a>uni-app 生命周期函数</h2><p>onLaunch    当uni-app 初始化完成时触发（全局只触发一次）<br>onShow    当 uni-app 启动，或从后台进入前台显示<br>onHide    当 uni-app 从前台进入后台<br>onUniNViewMessage    对 nvue 页面发送的数据进行监听，可参考 nvue 向 vue 通讯</p>
<h2 id="百度AI人脸识别、活体检测的坑"><a href="#百度AI人脸识别、活体检测的坑" class="headerlink" title="百度AI人脸识别、活体检测的坑"></a>百度AI人脸识别、活体检测的坑</h2><p>错误代码</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">222203  image check fail    无法解析人脸  检查图片质量</span><br></pre></td></tr></table></figure>

<p><code>data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAfQAAAH0CAYAAADL1t+KAAAgAElEQVR4Xo........</code></p>
<p>必须得把base64编码过的，把含有编码信息的头信息去掉</p>
<h2 id="EGG-怎么接入微信支付"><a href="#EGG-怎么接入微信支付" class="headerlink" title="EGG 怎么接入微信支付"></a>EGG 怎么接入微信支付</h2><p><a href="https://juejin.cn/post/6932389366618652679#heading-1" target="_blank" rel="noopener">微信V2和V3接口确定</a></p>
<p><a href="https://www.dazhuanlan.com/rem1x/topics/1173098" target="_blank" rel="noopener">https://www.dazhuanlan.com/rem1x/topics/1173098</a></p>
<p><a href="https://www.codenong.com/cs106531483/" target="_blank" rel="noopener">https://www.codenong.com/cs106531483/</a></p>
<p><a href="https://www.codeleading.com/article/44593117527/" target="_blank" rel="noopener">https://www.codeleading.com/article/44593117527/</a></p>
<p><a href="https://www.dazhuanlan.com/rem1x/topics/1173098" target="_blank" rel="noopener">https://www.dazhuanlan.com/rem1x/topics/1173098</a></p>
<p><a href="https://www.it610.com/article/1297241569925079040.htm" target="_blank" rel="noopener">NodeJS服务端实现微信小程序支付</a></p>
<p>提示签名失败：</p>
<p>检测参数！！！</p>
<p>按照文档的方式加密 </p>
<p>统一下单：<a href="https://pay.weixin.qq.com/wiki/doc/api/wxa/wxa_api.php?chapter=9_1" target="_blank" rel="noopener">https://pay.weixin.qq.com/wiki/doc/api/wxa/wxa_api.php?chapter=9_1</a></p>
<p>微信支付报签名错误sign error，如何排查？<br><a href="https://developers.weixin.qq.com/community/develop/doc/0000a4be9a8d5081d94b4e4c051c09" target="_blank" rel="noopener">https://developers.weixin.qq.com/community/develop/doc/0000a4be9a8d5081d94b4e4c051c09</a></p>
<h3 id="怎么接收微信的回调接口"><a href="#怎么接收微信的回调接口" class="headerlink" title="怎么接收微信的回调接口"></a>怎么接收微信的回调接口</h3><p>问题：egg的post中的body里没有数据<br>原因：微信返回的是xml的数据，但是egg里默认接受的是json的数据<br>egg 如何接收xml数据</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 覆盖egg自带的配置 使支持接收xml参数</span><br><span class="line">config.bodyParser &#x3D; &#123;</span><br><span class="line">  enable: true,</span><br><span class="line">  encoding: &#39;utf8&#39;,</span><br><span class="line">  formLimit: &#39;100kb&#39;,</span><br><span class="line">  jsonLimit: &#39;100kb&#39;,</span><br><span class="line">  strict: true,</span><br><span class="line">  &#x2F;&#x2F; @see https:&#x2F;&#x2F;github.com&#x2F;hapijs&#x2F;qs&#x2F;blob&#x2F;master&#x2F;lib&#x2F;parse.js#L8 for more options</span><br><span class="line">  queryString: &#123;</span><br><span class="line">    arrayLimit: 100,</span><br><span class="line">    depth: 5,</span><br><span class="line">    parameterLimit: 1000,</span><br><span class="line">  &#125;,</span><br><span class="line">  enableTypes: [&#39;json&#39;, &#39;form&#39;, &#39;text&#39;],</span><br><span class="line">  extendTypes: &#123;</span><br><span class="line">    text: [&#39;text&#x2F;xml&#39;, &#39;application&#x2F;xml&#39;],</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="微信小程序获取绑定授权用户手机号getPhoneNumber"><a href="#微信小程序获取绑定授权用户手机号getPhoneNumber" class="headerlink" title="微信小程序获取绑定授权用户手机号getPhoneNumber"></a>微信小程序获取绑定授权用户手机号getPhoneNumber</h2><p><a href="https://blog.csdn.net/weixin_41818665/article/details/79509792" target="_blank" rel="noopener">https://blog.csdn.net/weixin_41818665/article/details/79509792</a></p>
<p><a href="https://www.jb51.net/article/179007.htm" target="_blank" rel="noopener">微信小程序如何通过用户授权获取手机号(getPhoneNumber)</a></p>
<h2 id="小程序怎么在代码层面清除缓存"><a href="#小程序怎么在代码层面清除缓存" class="headerlink" title="小程序怎么在代码层面清除缓存"></a>小程序怎么在代码层面清除缓存</h2><p>分数据缓存和  登录状态缓存等</p>
<p>小程序只能清楚数据的缓存</p>
<h2 id="uniapp中uni-previewImage参数为base64类型图片预览在真机上不显示问题"><a href="#uniapp中uni-previewImage参数为base64类型图片预览在真机上不显示问题" class="headerlink" title="uniapp中uni.previewImage参数为base64类型图片预览在真机上不显示问题"></a>uniapp中uni.previewImage参数为base64类型图片预览在真机上不显示问题</h2><p>在字节小程序中有问题 是因为字节不支持base64的形式，微信小程序支持，改为image的方式展示</p>
<h2 id="Egg-js-实现向服务器上传图片"><a href="#Egg-js-实现向服务器上传图片" class="headerlink" title="Egg.js 实现向服务器上传图片"></a>Egg.js 实现向服务器上传图片</h2><p>可保存图片到本地环境</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">async saveImg (ctx) &#123;</span><br><span class="line">  const stream &#x3D; await this.userService.getFileStream();</span><br><span class="line">  &#x2F;&#x2F; 保存文件至本地</span><br><span class="line">  const currentTime &#x3D; moment();</span><br><span class="line">  const fileArray &#x3D; stream.filename.split(&#39;.&#39;);</span><br><span class="line">  const fileName &#x3D; &#96;$&#123;fileArray[0]&#125;-$&#123;currentTime.format(&#39;YYYYMMDDHHmmss&#39;)&#125;.$&#123;fileArray[1]&#125;&#96;;</span><br><span class="line">  const savePath &#x3D; ctx.app.config.fileUpload.savePath;</span><br><span class="line">  const writeFilePath &#x3D; path.join(__dirname, savePath + &#96;&#x2F;$&#123;fileName&#125;&#96;);</span><br><span class="line">  const writeBak &#x3D; path.join(__dirname, &#96;..&#x2F;..&#x2F;..&#x2F;uploadImg&#x2F;$&#123;fileName&#125;&#96;);</span><br><span class="line">  console.log(&quot;writeFilePath----&quot;, writeFilePath)</span><br><span class="line">  stream.pipe(await fs.createWriteStream(writeFilePath, &#123; encoding: &#39;utf-8&#39; &#125;))</span><br><span class="line">  stream.pipe(await fs.createWriteStream(writeBak, &#123; encoding: &#39;utf-8&#39; &#125;))</span><br><span class="line">  return ctx.body &#x3D; &#123;</span><br><span class="line">    code: 200,</span><br><span class="line">    message: &#39;图片上传成功&#39;,</span><br><span class="line">    fileName: fileName,</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">async getFileStream(): Promise&lt;any&gt; &#123;</span><br><span class="line">  const &#123; ctx &#125; &#x3D; this;</span><br><span class="line">  const stream &#x3D; await ctx.getFileStream();</span><br><span class="line"></span><br><span class="line">  return stream;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="https://www.cnblogs.com/crazycode2/p/12481854.html" target="_blank" rel="noopener">https://www.cnblogs.com/crazycode2/p/12481854.html</a></p>
<p><a href="https://blog.csdn.net/qq_33559093/article/details/95483498" target="_blank" rel="noopener">https://blog.csdn.net/qq_33559093/article/details/95483498</a></p>
<h2 id="uni-app里用v-html渲染的图片怎么改大小"><a href="#uni-app里用v-html渲染的图片怎么改大小" class="headerlink" title="uni-app里用v-html渲染的图片怎么改大小"></a>uni-app里用v-html渲染的图片怎么改大小</h2><ol>
<li>在全局样式中定义 加important</li>
<li>直接通过正则匹配图片添加样式</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;style&gt;&lt;&#x2F;style&gt;不要用scoped修饰。打开样式只能在当前组件内使用的限制</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;style scoped&gt;</span><br><span class="line">&gt;&gt;&gt; .imgWidth img &#123;</span><br><span class="line">  width: 100%;</span><br><span class="line">&#125;</span><br><span class="line">&lt;&#x2F;style&gt;</span><br></pre></td></tr></table></figure>

<h2 id="字节小程序自定义头部失效"><a href="#字节小程序自定义头部失效" class="headerlink" title="字节小程序自定义头部失效"></a>字节小程序自定义头部失效</h2><p>没有import Vue</p>
<p>uniapp小程序自定义头部</p>
<h2 id="字节小程序解密失败问题"><a href="#字节小程序解密失败问题" class="headerlink" title="字节小程序解密失败问题"></a>字节小程序解密失败问题</h2><p>在login和getUserInfo后首次登陆可以拿信息，但第二次登陆后解码失败</p>
<p>是因为没有清空登陆缓存</p>
<p>需要在字节开发者工具自行删除</p>
<p>uni.app开发字节跳动小程序的授权登陆</p>
<p>官方解码算法</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; Node.js</span><br><span class="line">const crypto &#x3D; require(&quot;crypto&quot;);</span><br><span class="line"></span><br><span class="line">function decrypt(encryptedData, sessionKey, iv) &#123;</span><br><span class="line">  const decipher &#x3D; crypto.createDecipheriv(</span><br><span class="line">    &quot;aes-128-cbc&quot;,</span><br><span class="line">    Buffer.from(sessionKey, &quot;base64&quot;),</span><br><span class="line">    Buffer.from(iv, &quot;base64&quot;)</span><br><span class="line">  );</span><br><span class="line">  let ret &#x3D; decipher.update(encryptedData, &quot;base64&quot;);</span><br><span class="line">  ret +&#x3D; decipher.final();</span><br><span class="line">  return ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="字节小程序担保支付接入文档-服务端"><a href="#字节小程序担保支付接入文档-服务端" class="headerlink" title="字节小程序担保支付接入文档-服务端"></a>字节小程序担保支付接入文档-服务端</h2><p>即接入微信或支付宝支付</p>
<p>流程1：createOrder  （签名算法）</p>
<p>流程2：前端使用tt.pay掉起收银台</p>
<p>流程3：支付成功后加入回调</p>
<p><a href="https://microapp.bytedance.com/docs/zh-CN/mini-app/develop/server/ecpay/server-doc/" target="_blank" rel="noopener">https://microapp.bytedance.com/docs/zh-CN/mini-app/develop/server/ecpay/server-doc/</a></p>
<p>坑：不清楚SALT的值</p>
<p><a href="https://182.92.158.184/articles/2021/05/23/1621749738396.html" target="_blank" rel="noopener">https://182.92.158.184/articles/2021/05/23/1621749738396.html</a></p>
<h2 id="如何设计表格？—-产品设计类"><a href="#如何设计表格？—-产品设计类" class="headerlink" title="如何设计表格？— 产品设计类"></a>如何设计表格？— 产品设计类</h2><p>结构简单，精准高效，使用简单</p>
<p><a href="http://lanlanwork.com/blog/m/?post=7054" target="_blank" rel="noopener">http://lanlanwork.com/blog/m/?post=7054</a></p>
<h2 id="SQL-中字符串匹配的用法"><a href="#SQL-中字符串匹配的用法" class="headerlink" title="SQL 中字符串匹配的用法"></a>SQL 中字符串匹配的用法</h2><p>关键字：like</p>
<p>%：匹配一个或多个字符；</p>
<p>_：匹配任意单个字符；</p>
<p>escape：定义转义字符。</p>
]]></content>
  </entry>
  <entry>
    <title>21-10月记录</title>
    <url>/21-10%E6%9C%88%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h2 id="如何在uni-app使用vuex"><a href="#如何在uni-app使用vuex" class="headerlink" title="如何在uni-app使用vuex"></a>如何在uni-app使用vuex</h2><p><a href="https://www.jianshu.com/p/c288b8e6067c" target="_blank" rel="noopener">https://www.jianshu.com/p/c288b8e6067c</a></p>
<h2 id="Mac下-Hbuilder-X使用uni-app创建微信小程序"><a href="#Mac下-Hbuilder-X使用uni-app创建微信小程序" class="headerlink" title="Mac下 Hbuilder X使用uni-app创建微信小程序"></a>Mac下 Hbuilder X使用uni-app创建微信小程序</h2><p><a href="https://blog.csdn.net/qq_42022466/article/details/106983008" target="_blank" rel="noopener">https://blog.csdn.net/qq_42022466/article/details/106983008</a></p>
<h2 id="微信小程序登录流程"><a href="#微信小程序登录流程" class="headerlink" title="微信小程序登录流程"></a>微信小程序登录流程</h2><p>先通过<code>wx.login()</code>的方式获取code<br>然后通过code去后台获取<code>session_key</code>和<code>openId</code></p>
<p>前台通过<code>session_key</code>判断是否过期</p>
<p>疑问：</p>
<p>为什么要通过这种方式？直接通过<code>wx.getUserProfile</code>的方式获取头像和姓名保存起来不就好了吗？</p>
<p>其实可能有一种方式，就是用户自己修改了名字和头像的情况下，用session_key的方式就可以获取到最新的头像，用户做的仅是重新授权</p>
<a id="more"></a>

<h2 id="微信小程序实现人脸识别注册登录"><a href="#微信小程序实现人脸识别注册登录" class="headerlink" title="微信小程序实现人脸识别注册登录"></a>微信小程序实现人脸识别注册登录</h2><p>参考1：使用百度api</p>
<p><a href="https://blog.csdn.net/qq_42543264/article/details/108151001" target="_blank" rel="noopener">https://blog.csdn.net/qq_42543264/article/details/108151001</a></p>
<p>参考2：微信小程序-拍照(人脸识别)</p>
<p><a href="https://blog.csdn.net/anyangjie11/article/details/80446224" target="_blank" rel="noopener">https://blog.csdn.net/anyangjie11/article/details/80446224</a></p>
<p>参考3：微信小程序-人脸识别（2)实现人脸识别功能</p>
<p>验证功能</p>
<p><a href="https://blog.csdn.net/qq_41856242/article/details/80557623" target="_blank" rel="noopener">https://blog.csdn.net/qq_41856242/article/details/80557623</a></p>
<h2 id="查看分销功能项目"><a href="#查看分销功能项目" class="headerlink" title="查看分销功能项目"></a>查看分销功能项目</h2><p>项目一：</p>
<p><a href="https://hub.fastgit.org/likeshop-github/likeshop" target="_blank" rel="noopener">likeshop单商户开源商城系统-企业版（免费商用）</a></p>
<p>项目二：</p>
<p><a href="https://hub.fastgit.org/niuteam/niushop" target="_blank" rel="noopener">Niushop开源商城单商户V4</a></p>
<p>项目三：</p>
<p><a href="https://hub.fastgit.org/CoreUnion/CoreShop" target="_blank" rel="noopener">coreShop</a></p>
<p>后台演示地址：可参考分销页面<br><a href="https://admin.demo.coreshop.cn/#/distribution/distributor/" target="_blank" rel="noopener">https://admin.demo.coreshop.cn/#/distribution/distributor/</a></p>
<p>项目四：</p>
<p><a href="https://hub.fastgit.org/guchengwuyue/yshopmall" target="_blank" rel="noopener">yshop意象商城系统</a></p>
<p>后台演示地址：可参考分销页面<br><a href="https://admin.demo.coreshop.cn/#/distribution/distributor/" target="_blank" rel="noopener">https://admin.demo.coreshop.cn/#/distribution/distributor/</a></p>
<p>分销后台功能设计可参考：<br><a href="https://zhuanlan.zhihu.com/p/153234617" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/153234617</a></p>
<p>电商产品设计：如何设计产品分销体系<br><a href="https://juejin.cn/post/6940572855042048030#heading-3" target="_blank" rel="noopener">https://juejin.cn/post/6940572855042048030#heading-3</a></p>
<h2 id="佣金计算方式："><a href="#佣金计算方式：" class="headerlink" title="佣金计算方式："></a>佣金计算方式：</h2><p>银行：</p>
<p><a href="https://wenku.baidu.com/view/acb2fd915bcfa1c7aa00b52acfc789eb162d9eba.html" target="_blank" rel="noopener">https://wenku.baidu.com/view/acb2fd915bcfa1c7aa00b52acfc789eb162d9eba.html</a></p>
<h3 id="java递归解决多级分销算法"><a href="#java递归解决多级分销算法" class="headerlink" title="java递归解决多级分销算法"></a>java递归解决多级分销算法</h3><p><a href="https://blog.csdn.net/qq_35624642/article/details/54235472" target="_blank" rel="noopener">https://blog.csdn.net/qq_35624642/article/details/54235472</a></p>
<h3 id="三级分销"><a href="#三级分销" class="headerlink" title="三级分销"></a>三级分销</h3><p>三级分销系统计算公式</p>
<p>所有的后台多有设置佣金的地方，你可以根据你的产品利润空间设置</p>
<p>例如：某商城的1件产品售价为100元，现商家拿出20元利润作为三级分销奖励给推广员，</p>
<p>即总佣金比例为0.2。</p>
<p>并且设置三级分佣的比例为：0.2、0.3、0.5</p>
<p>如果：粉丝e购买了这件100元的产品<br>那么：推广员D获得一级佣金100<em>0.2</em>0.5=10元;<br>推广员C获得二级佣金：100<em>0.2</em>0.3=6元;<br>推广员B获得三级佣金：100<em>0.2</em>0.2=4元;<br>推广员A已超过三级范围，故不获得佣金!</p>
<h2 id="微信小程序商品怎么生成二维码"><a href="#微信小程序商品怎么生成二维码" class="headerlink" title="微信小程序商品怎么生成二维码"></a>微信小程序商品怎么生成二维码</h2><p><a href="https://developers.weixin.qq.com/miniprogram/introduction/qrcode.html#%E5%8A%9F%E8%83%BD%E4%BB%8B%E7%BB%8D" target="_blank" rel="noopener">https://developers.weixin.qq.com/miniprogram/introduction/qrcode.html#%E5%8A%9F%E8%83%BD%E4%BB%8B%E7%BB%8D</a></p>
<h2 id="open-data标签"><a href="#open-data标签" class="headerlink" title="open-data标签"></a>open-data标签</h2><p>可直接访问</p>
<p>userNickName    用户昵称</p>
<p>userAvatarUrl    用户头像</p>
<p>userGender    用户性别</p>
<p>groupName    拉取群名称</p>
<p>userCity    用户所在城市</p>
<p>userProvince    用户所在省份</p>
<p>userCountry    用户所在国家</p>
<p>userLanguage    用户的语言</p>
<h2 id="jwt鉴权"><a href="#jwt鉴权" class="headerlink" title="jwt鉴权"></a>jwt鉴权</h2><p><a href="https://blog.tcs-y.com/2019/11/27/midway-jwt/" target="_blank" rel="noopener">midway jwt token 探索</a></p>
<p><a href="https://blog.csdn.net/qq_32674347/article/details/104117594" target="_blank" rel="noopener">egg-jwt egg jwt 使用</a></p>
<h2 id="Mac下MySql突然启动不了点启动反应"><a href="#Mac下MySql突然启动不了点启动反应" class="headerlink" title="Mac下MySql突然启动不了点启动反应"></a>Mac下MySql突然启动不了点启动反应</h2><p>由于好长一段时间没有使用MySql,去启动MySql时，突然启动不了，点启动没的反映。<br>网上查原因可能是：<br>原因是某种情况下导致”/usr/local/MySQL/data”的拥有者发生了改变，所以只需要将其拥有者修改为“mysql“就可以了。<br>在终端中运行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo chown -R mysql &#x2F;usr&#x2F;local&#x2F;mysql&#x2F;data</span><br></pre></td></tr></table></figure>

<p>chown用于更改文件的所有人和组，chmod用于更改文件的读写和执行权限，-R 处理指定目录以及其子目录下的所有文件</p>
<h2 id="uni-app踩坑-gitignore忽略提交unpackage文件夹至git仓库"><a href="#uni-app踩坑-gitignore忽略提交unpackage文件夹至git仓库" class="headerlink" title="uni-app踩坑 - gitignore忽略提交unpackage文件夹至git仓库"></a>uni-app踩坑 - gitignore忽略提交unpackage文件夹至git仓库</h2><p>首先命令行新建.gitignore文件。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">touch .gitignore</span><br></pre></td></tr></table></figure>

<p>然后记事本打开。<br>复制粘贴以下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">node_modules&#x2F;</span><br><span class="line">.project</span><br><span class="line">unpackage&#x2F;</span><br><span class="line">.DS_Store</span><br></pre></td></tr></table></figure>
<p>也可以直接使用uni-app的hello-uniapp模板中的.gitignore文件，仓库地址是：<a href="https://gitee.com/dcloud/hello-uniapp/blob/master/.gitignore" target="_blank" rel="noopener">DCloud/hello-uniapp/.gitignore</a></p>
<p>注意：如果之前就已经提交过unpackage文件夹，那需要执行以下命令行：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git rm -r --cached unpackage</span><br></pre></td></tr></table></figure>

<h2 id="【WebAR】开源项目：微信小程序AR实现颜色跟踪、人脸检测、图像识别、对象跟踪"><a href="#【WebAR】开源项目：微信小程序AR实现颜色跟踪、人脸检测、图像识别、对象跟踪" class="headerlink" title="【WebAR】开源项目：微信小程序AR实现颜色跟踪、人脸检测、图像识别、对象跟踪"></a>【WebAR】开源项目：微信小程序AR实现颜色跟踪、人脸检测、图像识别、对象跟踪</h2><h2 id="egg-js-加密操作-crypto"><a href="#egg-js-加密操作-crypto" class="headerlink" title="egg.js 加密操作 crypto"></a>egg.js 加密操作 crypto</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#39;use strict&#39;;</span><br><span class="line"></span><br><span class="line">const crypto &#x3D; require(&#39;crypto&#39;);</span><br><span class="line"></span><br><span class="line">class HomeController &#123;</span><br><span class="line">	</span><br><span class="line">	async md5Ceshi() &#123;</span><br><span class="line">		let &#x3D; str &#x3D; &#39;asdasdasdasda&#39;;</span><br><span class="line"></span><br><span class="line">		str &#x3D; crypto.createHash(&#39;md5&#39;).update(str).digest(&#39;hex&#39;);</span><br><span class="line">		ctx.body &#x3D; str;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">module.exports &#x3D; HomeController;</span><br></pre></td></tr></table></figure>

<h2 id="egg怎么实现jwt鉴权"><a href="#egg怎么实现jwt鉴权" class="headerlink" title="egg怎么实现jwt鉴权"></a>egg怎么实现jwt鉴权</h2><p>参考：</p>
<p><a href="https://www.lmlphp.com/user/16514/article/item/467701/" target="_blank" rel="noopener">https://www.lmlphp.com/user/16514/article/item/467701/</a></p>
<p><a href="https://www.jianshu.com/p/22a13040ae5e" target="_blank" rel="noopener">https://www.jianshu.com/p/22a13040ae5e</a></p>
<h2 id="uni-app开发前需看"><a href="#uni-app开发前需看" class="headerlink" title="uni-app开发前需看"></a>uni-app开发前需看</h2><p><a href="https://juejin.cn/post/7020680215009427470#heading-32" target="_blank" rel="noopener">😭血泪合集！uniapp小程序开发的超长实践总结！</a></p>
<h2 id="封装微信小程序接口"><a href="#封装微信小程序接口" class="headerlink" title="封装微信小程序接口"></a>封装微信小程序接口</h2><p><a href="https://blog.csdn.net/agax0312/article/details/82804601" target="_blank" rel="noopener">https://blog.csdn.net/agax0312/article/details/82804601</a></p>
<h3 id="antd-Form表单中给Radio-Group设置默认选中值"><a href="#antd-Form表单中给Radio-Group设置默认选中值" class="headerlink" title="antd Form表单中给Radio.Group设置默认选中值"></a>antd Form表单中给Radio.Group设置默认选中值</h3><p><a href="https://blog.csdn.net/weixin_41796119/article/details/106571150" target="_blank" rel="noopener">https://blog.csdn.net/weixin_41796119/article/details/106571150</a></p>
<h2 id="mysql如果select字段少会快点吗"><a href="#mysql如果select字段少会快点吗" class="headerlink" title="mysql如果select字段少会快点吗"></a>mysql如果select字段少会快点吗</h2><p><a href="https://blog.csdn.net/weixin_36469682/article/details/113401068" target="_blank" rel="noopener">https://blog.csdn.net/weixin_36469682/article/details/113401068</a></p>
<h2 id="js结构赋值怎么改变量名"><a href="#js结构赋值怎么改变量名" class="headerlink" title="js结构赋值怎么改变量名"></a>js结构赋值怎么改变量名</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let &#123; foo: baz &#125; &#x3D; &#123; foo: &#39;aaa&#39;, bar: &#39;bbb&#39; &#125;;  &#x2F;&#x2F;  此时foo是匹配的模式，baz才是变量。真正被赋值的是变量baz，而不是模式foo</span><br><span class="line">baz &#x2F;&#x2F; ’aaa’</span><br><span class="line">foo &#x2F;&#x2F; error: foo is not defined</span><br></pre></td></tr></table></figure>

<p><a href="https://blog.csdn.net/weixin_43912756/article/details/108091873" target="_blank" rel="noopener">https://blog.csdn.net/weixin_43912756/article/details/108091873</a></p>
<h2 id="分销商等级是什么"><a href="#分销商等级是什么" class="headerlink" title="分销商等级是什么"></a>分销商等级是什么</h2><p><a href="https://jingyan.baidu.com/article/a948d65125b7cd0a2ccd2e6d.html" target="_blank" rel="noopener">https://jingyan.baidu.com/article/a948d65125b7cd0a2ccd2e6d.html</a></p>
<h2 id="SQL错误”in-field-list-is-ambiguous”解决办法"><a href="#SQL错误”in-field-list-is-ambiguous”解决办法" class="headerlink" title="SQL错误”in field list is ambiguous”解决办法"></a>SQL错误”in field list is ambiguous”解决办法</h2><p>原因：俩个表有相同的字段,然而我们没有进行区分UIDPK是哪张表的字段,所以会出来上面的错误</p>
<h2 id="moment时间对不上的问题："><a href="#moment时间对不上的问题：" class="headerlink" title="moment时间对不上的问题："></a>moment时间对不上的问题：</h2><p>如：提交2020-01-10 14:30:00，然后返回的是2020-01-10 02:30:00</p>
<p>刚开会以为是后端的问题，后来仔细查看，发现是自己的问题。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">moment(values.data).format(&#39;YYYY-MM-DD HH:mm:ss&#39;)--------------24小时</span><br><span class="line">moment(values.data).format(&#39;YYYY-MM-DD hh:mm:ss&#39;)--------------12小时</span><br></pre></td></tr></table></figure>
<p>主要取决于format中的时分秒的大小写</p>
<h2 id="SQL按时间排序"><a href="#SQL按时间排序" class="headerlink" title="SQL按时间排序"></a>SQL按时间排序</h2><p>Order By命令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">select top 1 * from MyTable Order By ModifyTime Desc</span><br></pre></td></tr></table></figure>

<h2 id="JS生成订单号"><a href="#JS生成订单号" class="headerlink" title="JS生成订单号"></a>JS生成订单号</h2><p>两种方式</p>
<p><a href="https://blog.csdn.net/qq_38209578/article/details/100654560" target="_blank" rel="noopener">https://blog.csdn.net/qq_38209578/article/details/100654560</a></p>
<h2 id="node搭建本地文件服务器"><a href="#node搭建本地文件服务器" class="headerlink" title="node搭建本地文件服务器"></a>node搭建本地文件服务器</h2><p><a href="https://juejin.cn/post/6847902219745001485" target="_blank" rel="noopener">https://juejin.cn/post/6847902219745001485</a></p>
<h3 id="minio"><a href="#minio" class="headerlink" title="minio"></a>minio</h3><p>搭建minio</p>
<p><a href="https://www.cnblogs.com/Mufasa/p/13983023.html" target="_blank" rel="noopener">基于MinIO JavaSDK实现的文件管理系统
</a></p>
<p><a href="https://blog.csdn.net/u010651369/article/details/120269441" target="_blank" rel="noopener">mino设置成服务开机启动</a></p>
<h2 id="Lodash-pick和omit"><a href="#Lodash-pick和omit" class="headerlink" title="Lodash pick和omit"></a>Lodash pick和omit</h2><p><a href="https://blog.csdn.net/suwu150/article/details/75250749" target="_blank" rel="noopener">https://blog.csdn.net/suwu150/article/details/75250749</a></p>
<h2 id="mysql如果某一个字段数据很大-对查询速度会有影响吗"><a href="#mysql如果某一个字段数据很大-对查询速度会有影响吗" class="headerlink" title="mysql如果某一个字段数据很大 对查询速度会有影响吗"></a>mysql如果某一个字段数据很大 对查询速度会有影响吗</h2><p><a href="https://www.zhihu.com/question/41730555?sort=created" target="_blank" rel="noopener">https://www.zhihu.com/question/41730555?sort=created</a></p>
<p>建议分表</p>
<h2 id="http-content-type"><a href="#http-content-type" class="headerlink" title="http content-type"></a>http content-type</h2><p>application/json和form的方式哪个能接受的数据量大？？</p>
<h2 id="egg-js怎么自动重启服务"><a href="#egg-js怎么自动重启服务" class="headerlink" title="egg.js怎么自动重启服务"></a>egg.js怎么自动重启服务</h2><h2 id="pm2运行egg-js服务"><a href="#pm2运行egg-js服务" class="headerlink" title="pm2运行egg.js服务"></a>pm2运行egg.js服务</h2><p><a href="https://www.fed123.com/pwa/5809.html" target="_blank" rel="noopener">https://www.fed123.com/pwa/5809.html</a></p>
<h2 id="使用node完成文件自动拷贝"><a href="#使用node完成文件自动拷贝" class="headerlink" title="使用node完成文件自动拷贝"></a>使用node完成文件自动拷贝</h2><p><a href="https://itbilu.com/nodejs/core/41cswxaTx.html" target="_blank" rel="noopener">https://itbilu.com/nodejs/core/41cswxaTx.html</a></p>
<p>使用linux的命令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var child_process &#x3D; require(&#39;child_process&#39;);</span><br><span class="line"></span><br><span class="line">function copyDir(src, dist) &#123;</span><br><span class="line">  child_process.spawn(&#39;cp&#39;, [&#39;-r&#39;, src, dist]);	</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">copyDir(&#39;.&#x2F;test&#39;, &#39;.&#x2F;new&#39;);</span><br></pre></td></tr></table></figure>

<h2 id="yarn-–prod是什么意思"><a href="#yarn-–prod是什么意思" class="headerlink" title="yarn –prod是什么意思"></a>yarn –prod是什么意思</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yarn install --production[&#x3D;true|false]</span><br></pre></td></tr></table></figure>

<p>如果 NODE_ENV 环境变量设为 production，Yarn 将不安装任何列于 devDependencies 的包。 使用此标志指示 Yarn 忽略 NODE_ENV 并用它取代“生产”与否的状态。</p>
<p>注意： –production 等同 –production=true。–prod 是 –production 的别名。</p>
<h2 id="微信小程序encryptedData解码失败"><a href="#微信小程序encryptedData解码失败" class="headerlink" title="微信小程序encryptedData解码失败"></a>微信小程序encryptedData解码失败</h2><p>前端需先调用login然后再getUserInfoProfiler获取数据</p>
<p>因为前端在调用getPhoneNumber成功之后回调wx.login去登录，这就导致了登录被刷新，此时服务器使用 code 换取的 sessionKey 不是加密时使用的 sessionKey，导致解密失败。建议开发者提前进行 login；或者在回调中先使用 checkSession 进行登录态检查，避免 login 刷新登录态。</p>
<p>解决办法：优先调用wx.login,然后再去调用其他接口。</p>
<p>具体写法可参考：</p>
<p><a href="https://viencoding.com/article/300" target="_blank" rel="noopener">https://viencoding.com/article/300</a></p>
<h2 id="如何将emoji表情存放到mysql数据库中"><a href="#如何将emoji表情存放到mysql数据库中" class="headerlink" title="如何将emoji表情存放到mysql数据库中"></a>如何将emoji表情存放到mysql数据库中</h2><p>改成UTF8MB4的形式，而且egg-mysql和数据库类型都要改</p>
<h2 id="mysql查看conf文件"><a href="#mysql查看conf文件" class="headerlink" title="mysql查看conf文件"></a>mysql查看conf文件</h2><p><code>mysql --help|grep &#39;my.cnf&#39;</code></p>
<h2 id="egg-mysql怎么回滚插入操作"><a href="#egg-mysql怎么回滚插入操作" class="headerlink" title="egg-mysql怎么回滚插入操作"></a>egg-mysql怎么回滚插入操作</h2><p>使用事务（未尝试）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const conn &#x3D; await app.mysql.beginTransaction(); &#x2F;&#x2F; 初始化事务</span><br><span class="line"></span><br><span class="line">try &#123;</span><br><span class="line">  await conn.insert(table, row1);  &#x2F;&#x2F; 第一步操作</span><br><span class="line">  await conn.update(table, row2);  &#x2F;&#x2F; 第二步操作</span><br><span class="line">  await conn.commit(); &#x2F;&#x2F; 提交事务</span><br><span class="line">&#125; catch (err) &#123;</span><br><span class="line">  &#x2F;&#x2F; error, rollback</span><br><span class="line">  await conn.rollback(); &#x2F;&#x2F; 一定记得捕获异常后回滚事务！！</span><br><span class="line">  throw err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ctx-curl返回的是buffer"><a href="#ctx-curl返回的是buffer" class="headerlink" title="ctx.curl返回的是buffer"></a>ctx.curl返回的是buffer</h2><p>dataType = ‘json’,</p>
<p>egg上面默认的dataType是buffer,这里改成json后,以后使用导出文件,要记得加上buffer类型</p>
<h2 id="node怎么判断字符串是base64格式的"><a href="#node怎么判断字符串是base64格式的" class="headerlink" title="node怎么判断字符串是base64格式的"></a>node怎么判断字符串是base64格式的</h2><p><a href="https://www.shuzhiduo.com/A/kPzOK6w1zx/" target="_blank" rel="noopener">https://www.shuzhiduo.com/A/kPzOK6w1zx/</a></p>
<h2 id="base64编码的图片如何存入数据库"><a href="#base64编码的图片如何存入数据库" class="headerlink" title="base64编码的图片如何存入数据库"></a>base64编码的图片如何存入数据库</h2><p>转化为本地图片存储</p>
<h2 id="node-将文件变为base64编码"><a href="#node-将文件变为base64编码" class="headerlink" title="node 将文件变为base64编码"></a>node 将文件变为base64编码</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">app.post(&#39;&#x2F;upload&#39;, function(req, res)&#123;</span><br><span class="line">    &#x2F;&#x2F;接收前台POST过来的base64</span><br><span class="line">    var imgData &#x3D; req.body.imgData;</span><br><span class="line">    &#x2F;&#x2F;过滤data:URL</span><br><span class="line">    var base64Data &#x3D; imgData.replace(&#x2F;^data:image\&#x2F;\w+;base64,&#x2F;, &quot;&quot;);</span><br><span class="line">    var dataBuffer &#x3D; new Buffer(base64Data, &#39;base64&#39;);</span><br><span class="line">    fs.writeFile(&quot;image.png&quot;, dataBuffer, function(err) &#123;</span><br><span class="line">        if(err)&#123;</span><br><span class="line">          res.send(err);</span><br><span class="line">        &#125;else&#123;</span><br><span class="line">          res.send(&quot;保存成功！&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><a href="https://blog.csdn.net/huyande123/article/details/109381584" target="_blank" rel="noopener">https://blog.csdn.net/huyande123/article/details/109381584</a></p>
<h2 id="NodeJs将任意文件转换为base64格式"><a href="#NodeJs将任意文件转换为base64格式" class="headerlink" title="NodeJs将任意文件转换为base64格式"></a>NodeJs将任意文件转换为base64格式</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const fs &#x3D; require(&#39;fs&#39;);</span><br><span class="line">const path &#x3D; require(&#39;path&#39;);</span><br><span class="line">const mineType &#x3D; require(&#39;mime-types&#39;);</span><br><span class="line"> </span><br><span class="line">let filePath &#x3D; path.resolve(&#39;your&#x2F;file&#x2F;path&#39;);</span><br><span class="line"> </span><br><span class="line">let data &#x3D; fs.readFileSync(filePath);</span><br><span class="line">data &#x3D; new Buffer(data).toString(&#39;base64&#39;);</span><br><span class="line"> </span><br><span class="line">let base64 &#x3D; &#39;data:&#39; + mineType.lookup(filePath) + &#39;;base64,&#39; + data;</span><br><span class="line"> </span><br><span class="line">fs.writeFileSync(path.resolve(&#39;your&#x2F;save&#x2F;file&#x2F;path&#39;), base64);</span><br></pre></td></tr></table></figure>

<h2 id="mysql之限定特定IP访问-使用本机ip失败"><a href="#mysql之限定特定IP访问-使用本机ip失败" class="headerlink" title="mysql之限定特定IP访问 使用本机ip失败"></a>mysql之限定特定IP访问 使用本机ip失败</h2><p>需用网络的ip，直接在百度搜索ip出现的绑定就可以</p>
<p><a href="https://blog.csdn.net/zmisno1/article/details/92787906" target="_blank" rel="noopener">https://blog.csdn.net/zmisno1/article/details/92787906</a></p>
<h2 id="redis-bind的误区"><a href="#redis-bind的误区" class="headerlink" title="redis bind的误区"></a>redis bind的误区</h2><p>不是指哪台机器能访问，是指哪个网卡的ip可以访问</p>
<p><a href="https://blog.csdn.net/cw_hello1/article/details/83444013" target="_blank" rel="noopener">https://blog.csdn.net/cw_hello1/article/details/83444013</a></p>
<h2 id="node怎么获取固定的文件夹路径"><a href="#node怎么获取固定的文件夹路径" class="headerlink" title="node怎么获取固定的文件夹路径"></a>node怎么获取固定的文件夹路径</h2><p>__dirname</p>
<p><a href="https://blog.csdn.net/naipeng/article/details/76047343" target="_blank" rel="noopener">dirname和basename用法</a></p>
<h2 id="js怎么把字符串的emoji表情去掉"><a href="#js怎么把字符串的emoji表情去掉" class="headerlink" title="js怎么把字符串的emoji表情去掉"></a>js怎么把字符串的emoji表情去掉</h2><p>通过正则</p>
<p><a href="https://juejin.cn/post/6844903593510715406" target="_blank" rel="noopener">https://juejin.cn/post/6844903593510715406</a></p>
<h2 id="使用moment-js怎么加减日期时间"><a href="#使用moment-js怎么加减日期时间" class="headerlink" title="使用moment.js怎么加减日期时间"></a>使用moment.js怎么加减日期时间</h2><p>add</p>
<p><a href="https://blog.csdn.net/weixin_43972758/article/details/104595029" target="_blank" rel="noopener">https://blog.csdn.net/weixin_43972758/article/details/104595029</a></p>
<h2 id><a href="#" class="headerlink" title></a></h2>]]></content>
  </entry>
  <entry>
    <title>7月记录</title>
    <url>/21-7%E6%9C%88%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>以下内容记录工作中遇到的一些问题，解决方法</p>
<h2 id="07-28"><a href="#07-28" class="headerlink" title="07-28"></a>07-28</h2><h3 id="JS从数组中获取前5个最大元素"><a href="#JS从数组中获取前5个最大元素" class="headerlink" title="JS从数组中获取前5个最大元素"></a>JS从数组中获取前5个最大元素</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">values &#x3D; [1,65,8,98,689,12,33,2,3,789];</span><br><span class="line"></span><br><span class="line">var topValues &#x3D; values.sort((a,b) &#x3D;&gt; b-a).slice(0,5);</span><br><span class="line"></span><br><span class="line">console.log(topValues); &#x2F;&#x2F; [ 1, 2, 3, 8, 12 ]</span><br></pre></td></tr></table></figure>

<h3 id="Echarts中markPoint大小随内容值变化"><a href="#Echarts中markPoint大小随内容值变化" class="headerlink" title="Echarts中markPoint大小随内容值变化"></a>Echarts中markPoint大小随内容值变化</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">markPoint: &#123;</span><br><span class="line">    &#x2F;&#x2F; 根据当前某项数据确定标注容器宽高。若为0，固定为40；若非0，其数值位数少于等于4位，固定为 66 ，否则 位数*14 作为其宽高</span><br><span class="line">    symbolSize: (e, e1) &#x3D;&gt; e ? e.toString().length &lt;&#x3D; 4 ? 66 : e.toString().length * 14 : 40,</span><br><span class="line">    data: [</span><br><span class="line">        &#123; type: &#39;max&#39;, name: &#39;高峰期&#39; &#125;,</span><br><span class="line">        &#123; type: &#39;min&#39;, name: &#39;空闲期&#39; &#125;</span><br><span class="line">    ],</span><br><span class="line">    label: &#123;</span><br><span class="line">        show: true,</span><br><span class="line">        color: &quot;#f00&quot;,&#x2F;&#x2F;气泡中字体颜色</span><br><span class="line">        fontSize: 12</span><br><span class="line">    &#125;</span><br><span class="line">&#125;,</span><br><span class="line"></span><br><span class="line">echarts - markPoint</span><br></pre></td></tr></table></figure>

<p>主要关注<code>symbolSize</code>属性</p>
<h3 id="markPoint的点击事件和点击事件的冲突"><a href="#markPoint的点击事件和点击事件的冲突" class="headerlink" title="markPoint的点击事件和点击事件的冲突"></a>markPoint的点击事件和点击事件的冲突</h3><p>问题1：</p>
<p>使用了chartIns.getZr().click()后，markPoint的点击事件不触发了。</p>
<p>markPoint的点击事件怎么触发？</p>
<p>解决:</p>
<p>使用chart.click()事件可以触发</p>
<p>问题2：</p>
<p>因为点击事件（多表联动）实际代码在getZr()中执行，click执行的是点击之后也需要获取点击的x轴数据进行多表联动。</p>
<p>因此顺序应该 -&gt; 点击后触发click()事件获取了对应的x值 -&gt; 再触发getZr()事件触发多表联动</p>
<p>但实际上的顺序是getZr()会更早被执行</p>
<p>解决：</p>
<p>方案1：猜测是因为冒泡事件的顺序问题导致，因此使用<code>e.stopPropagation()</code>但是没效果</p>
<p>方案2：利用async和await方法,用promise函数实现异步</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">chartIns.getZr().on(&#39;click&#39;, async (params) &#x3D;&gt; &#123;</span><br><span class="line">  let &#123; flag, tagParams &#125; &#x3D; await this.getHasTagClick()</span><br><span class="line">  &#x2F;&#x2F; 多表联动</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">getHasTagClick() &#123;</span><br><span class="line">  return new Promise((resolve, reject)&#x3D;&gt;&#123;</span><br><span class="line">    setTimeout(()&#x3D;&gt;&#123;</span><br><span class="line">      resolve(this.tagClick)</span><br><span class="line">    &#125;, 0)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<h3 id="JS阻止冒泡和取消默认事件-默认行为"><a href="#JS阻止冒泡和取消默认事件-默认行为" class="headerlink" title="JS阻止冒泡和取消默认事件(默认行为)"></a>JS阻止冒泡和取消默认事件(默认行为)</h3><ul>
<li>事件冒泡</li>
</ul>
<p>指的是阻止事件从最里层到外层不断执行click事件，如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div id&#x3D;&#39;div&#39; onclick&#x3D;&#39;alert(&quot;div&quot;);&#39;&gt;</span><br><span class="line">  &lt;ul onclick&#x3D;&#39;alert(&quot;ul&quot;);&#39;&gt;</span><br><span class="line">    &lt;li onclick&#x3D;&#39;alert(&quot;li&quot;);&#39;&gt;test&lt;&#x2F;li&gt;</span><br><span class="line">  &lt;&#x2F;ul&gt;</span><br><span class="line">&lt;&#x2F;div&gt;</span><br></pre></td></tr></table></figure>

<p>如果不组织冒泡，会依次触发alert(“li”),alert(“ul”),alert(“div”)</p>
<p>w3c的方法是e.stopPropagation()，IE则是使用e.cancelBubble = true</p>
<p>stopPropagation阻止目标元素的事件冒泡到父级元素</p>
<ul>
<li>默认事件</li>
</ul>
<p>指的是标签的默认事件或默认行为</p>
<p>如链接<code>&lt;a&gt;</code>，提交按钮<code>&lt;input type=”submit”&gt;</code>等。当Event 对象的 cancelable为false时，表示没有默认行为，这时即使有默认行为。</p>
<p>w3c的方法是e.preventDefault()，IE则是使用e.returnValue = false;</p>
<h3 id="ES6中判断对象是否存在某属性"><a href="#ES6中判断对象是否存在某属性" class="headerlink" title="ES6中判断对象是否存在某属性"></a>ES6中判断对象是否存在某属性</h3><h4 id="链判断操作符"><a href="#链判断操作符" class="headerlink" title="链判断操作符(?.)"></a>链判断操作符(?.)</h4><p>是否存在对象属性(不存在返回undefined且不再往下执行)</p>
<ul>
<li>对象属性：obj?.prop、obj?.[expr]</li>
<li>函数调用：func?.(…args)</li>
</ul>
<h4 id="空判断操作符"><a href="#空判断操作符" class="headerlink" title="空判断操作符(??)"></a>空判断操作符(??)</h4><p>是否值为undefined或null，是则使用默认值</p>
<p>只有运算符左侧的值为null或undefined时，才会返回右侧的值</p>
<p>const animationDuration = response.settings ?? 300;</p>
<p>如果多个逻辑运算符一起使用，必须用括号表明优先级，否则会报错。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">lhs &amp;&amp; middle) ?? rhs;</span><br><span class="line">lhs &amp;&amp; (middle ?? rhs);</span><br><span class="line"></span><br><span class="line">(lhs ?? middle) &amp;&amp; rhs;</span><br><span class="line">lhs ?? (middle &amp;&amp; rhs);</span><br><span class="line"></span><br><span class="line">(lhs || middle) ?? rhs;</span><br><span class="line">lhs || (middle ?? rhs);</span><br><span class="line"></span><br><span class="line">(lhs ?? middle) || rhs;</span><br></pre></td></tr></table></figure>

<p><strong>注意：但是这些需要用babel解析成es5才能被浏览器解析</strong></p>
<p><a href="https://segmentfault.com/a/1190000020678240" target="_blank" rel="noopener">1.5万字概括ES6全部特性(已更新ES2020)</a></p>
<h3 id="JS的发布订阅模式"><a href="#JS的发布订阅模式" class="headerlink" title="JS的发布订阅模式"></a>JS的发布订阅模式</h3><p>项目中使用多表联动，用发布订阅模式封装代码是最好的</p>
<p>发布-订阅模式里面包含了三个模块，发布者，订阅者和处理中心。这里处理中心相当于报刊办事大厅。发布者相当与某个杂志负责人，他来中心这注册一个的杂志，而订阅者相当于用户，我在中心订阅了这分杂志。每当发布者发布了一期杂志，办事大厅就会通知订阅者来拿新杂志。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Event &#123;</span><br><span class="line">  constructor () &#123;&#125;</span><br><span class="line">  &#x2F;&#x2F; 首先定义一个事件容器，用来装事件数组（因为订阅者可以是多个）</span><br><span class="line">  handlers &#x3D; &#123;&#125;</span><br><span class="line"></span><br><span class="line">  &#x2F;&#x2F; 事件添加方法，参数有事件名和事件方法</span><br><span class="line">  addEventListener (type, handler) &#123;</span><br><span class="line">    &#x2F;&#x2F; 首先判断handlers内有没有type事件容器，没有则创建一个新数组容器</span><br><span class="line">    if (!(type in this.handlers)) &#123;</span><br><span class="line">      this.handlers[type] &#x3D; []</span><br><span class="line">    &#125;</span><br><span class="line">    &#x2F;&#x2F; 将事件存入</span><br><span class="line">    this.handlers[type].push(handler)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  &#x2F;&#x2F; 触发事件两个参数（事件名，参数）</span><br><span class="line">  dispatchEvent (type, ...params) &#123;</span><br><span class="line">    &#x2F;&#x2F; 若没有注册该事件则抛出错误</span><br><span class="line">    if (!(type in this.handlers)) &#123;</span><br><span class="line">      return new Error(&#39;未注册该事件&#39;)</span><br><span class="line">    &#125;</span><br><span class="line">    &#x2F;&#x2F; 便利触发</span><br><span class="line">    this.handlers[type].forEach(handler &#x3D;&gt; &#123;</span><br><span class="line">      handler(...params)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  &#x2F;&#x2F; 事件移除参数（事件名，删除的事件，若无第二个参数则删除该事件的订阅和发布）</span><br><span class="line">  removeEventListener (type, handler) &#123;</span><br><span class="line">      &#x2F;&#x2F; 无效事件抛出</span><br><span class="line">      if (!(type in this.handlers)) &#123;</span><br><span class="line">        return new Error(&#39;无效事件&#39;)</span><br><span class="line">      &#125;</span><br><span class="line">      if (!handler) &#123;</span><br><span class="line">        &#x2F;&#x2F; 直接移除事件</span><br><span class="line">        delete this.handlers[type]</span><br><span class="line">      &#125; else &#123;</span><br><span class="line">        const idx &#x3D; this.handlers[type].findIndex(ele &#x3D;&gt; ele &#x3D;&#x3D;&#x3D; handler)</span><br><span class="line">        &#x2F;&#x2F; 抛出异常事件</span><br><span class="line">        if (idx &#x3D;&#x3D;&#x3D; undefined) &#123;</span><br><span class="line">          return new Error(&#39;无该绑定事件&#39;)</span><br><span class="line">        &#125;</span><br><span class="line">        &#x2F;&#x2F; 移除事件</span><br><span class="line">        this.handlers[type].splice(idx, 1)</span><br><span class="line">        if (this.handlers[type].length &#x3D;&#x3D;&#x3D; 0) &#123;</span><br><span class="line">          delete this.handlers[type]</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>调用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var event &#x3D; new Event() &#x2F;&#x2F; 创建event实例</span><br><span class="line">&#x2F;&#x2F; 定义一个自定义事件:&quot;load&quot;</span><br><span class="line">function load (params) &#123;</span><br><span class="line">  console.log(&#39;load&#39;, params)</span><br><span class="line">&#125;</span><br><span class="line">event.addEventListener(&#39;load&#39;, load)</span><br><span class="line">&#x2F;&#x2F; 再定义一个load事件</span><br><span class="line">function load2 (params) &#123;</span><br><span class="line">  console.log(&#39;load2&#39;, params)</span><br><span class="line">&#125;</span><br><span class="line">event.addEventListener(&#39;load&#39;, load2)</span><br><span class="line">&#x2F;&#x2F; 触发该事件</span><br><span class="line">event.dispatchEvent(&#39;load&#39;, &#39;load事件触发&#39;)</span><br><span class="line">&#x2F;&#x2F; 移除load2事件</span><br><span class="line">event.removeEventListener(&#39;load&#39;, load2)</span><br><span class="line">&#x2F;&#x2F; 移除所有load事件</span><br><span class="line">event.removeEventListener(&#39;load&#39;)</span><br></pre></td></tr></table></figure>

<h3 id="npm常用模块之cross-env使用"><a href="#npm常用模块之cross-env使用" class="headerlink" title="npm常用模块之cross-env使用"></a>npm常用模块之cross-env使用</h3><p>cross-env这是一款运行跨平台设置和使用环境变量的脚本</p>
<p>NODE_ENV=production像这样设置环境变量时，大多数Windows命令提示符都会阻塞 。（Windows上的Bash是例外，它使用本机Bash。）同样，Windows和POSIX命令使用环境变量的方式也有所不同。对于POSIX，您可以使用：$ENV_VAR 和在Windows上可以使用%ENV_VAR%。</p>
<p>cross-env这样，您就可以使用单个命令，而不必担心为平台正确设置或使用环境变量。就像在POSIX系统上运行时一样，只需对其进行设置即可，并且cross-env会妥善设置。</p>
<h3 id="Echarts去掉markPoint点"><a href="#Echarts去掉markPoint点" class="headerlink" title="Echarts去掉markPoint点"></a>Echarts去掉markPoint点</h3><p>把markPoint的data置为<code>[]</code></p>
<p>在Echarts5中，产生markPoint后再缩放，再点击legend的其他选项</p>
<p>之前的点会遗留在图表上（可能是echarts5的bug）</p>
<p>解决：放到echarts4版本上没问题</p>
<h3 id="生产环境打包后image-gallery组件提示-Super-expression-must-either-be-null-or-a-function"><a href="#生产环境打包后image-gallery组件提示-Super-expression-must-either-be-null-or-a-function" class="headerlink" title="生产环境打包后image-gallery组件提示 Super expression must either be null or a function"></a>生产环境打包后image-gallery组件提示 Super expression must either be null or a function</h3><p>问题出现：</p>
<p>经过webpack打包后，development环境下没问题，但是到了生产环境上出现上述错误</p>
<p>百度后:</p>
<p>回答1：组件继承的写法有问题：</p>
<p>错误写法：<br><code>class Fn extends React.Component() {}</code></p>
<p><code>class Fn extends React.component {}</code></p>
<p>正确写法：<br><code>class Fn extends React.Component {}</code></p>
<p>但排查后没问题</p>
<p>自查：</p>
<p>排查后发现是webpack打包中因为<code>mode</code>改成<code>production</code>后出现问题，在生产环境的配置中把<code>mode: development</code>后执行没问题。</p>
<p>因此，需要了解这两个模式做了什么</p>
<p>最终解决：</p>
<p>使用<code>UglifyjsWebpackPlugin</code>代替<code>TerserWebpackPlugin</code>进行代码压缩的配置</p>
<h3 id="webpakc中mode的区别"><a href="#webpakc中mode的区别" class="headerlink" title="webpakc中mode的区别"></a>webpakc中mode的区别</h3><p>官网解释：</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>development</td>
<td>会将 DefinePlugin 中 process.env.NODE_ENV 的值设置为 development。启用 NamedChunksPlugin 和 NamedModulesPlugin。</td>
</tr>
<tr>
<td>production</td>
<td>会将 DefinePlugin 中 process.env.NODE_ENV 的值设置为 production。启用 FlagDependencyUsagePlugin, FlagIncludedChunksPlugin, ModuleConcatenationPlugin, NoEmitOnErrorsPlugin, OccurrenceOrderPlugin, SideEffectsFlagPlugin 和 TerserPlugin。</td>
</tr>
<tr>
<td>none</td>
<td>退出任何默认优化选项</td>
</tr>
</tbody></table>
<p><a href="https://v4.webpack.docschina.org/concepts/mode/#mode-production" target="_blank" rel="noopener">webpack官网解释</a></p>
<p>在正式版本中，所省略的插件，如下所示:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; webpack.production.config.js</span><br><span class="line">module.exports &#x3D; &#123;</span><br><span class="line">+  mode: &#39;production&#39;,</span><br><span class="line">-  plugins: [</span><br><span class="line">-    new UglifyJsPlugin(&#x2F;* ... *&#x2F;),</span><br><span class="line">-    new webpack.DefinePlugin(&#123; &quot;process.env.NODE_ENV&quot;: JSON.stringify(&quot;production&quot;) &#125;),</span><br><span class="line">-    new webpack.optimize.ModuleConcatenationPlugin(),</span><br><span class="line">-    new webpack.NoEmitOnErrorsPlugin()</span><br><span class="line">-  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="UglifyJsPlugin"><a href="#UglifyJsPlugin" class="headerlink" title="UglifyJsPlugin"></a>UglifyJsPlugin</h4><p>我们第一个需要处理的就要混淆&amp;压缩JS了吧，这个时候就要请出<code>UglifyJs</code>了，在webpack中他的名字是<code>const UglifyJsPlugin = require(&#39;uglifyjs-webpack-plugin&#39;);</code>，这样就可以使用他了。</p>
<p>不过<code>new UglifyJsPlugin()</code>，这个插件我们可以在<code>optimize</code>中配置，效果是一样的，那么我们是不是就不用再导入一个新的插件了，这样反而会拖慢webpack的就打包速度。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">optimization:&#123;</span><br><span class="line">    minimize: true,</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<p>将插件去除，混淆压缩放入optimization，这样webpack速度快的飞起了。只有第一次打包会慢，之后再打包就快了</p>
<h4 id="ModuleConcatenationPlugin"><a href="#ModuleConcatenationPlugin" class="headerlink" title="ModuleConcatenationPlugin"></a>ModuleConcatenationPlugin</h4><p><code>webpack.optimize.ModuleConcatenationPlugin()</code>这个插件的作用是什么呢？官方文档上是这么描述的：</p>
<blockquote>
<p>记住，此插件仅适用于由 webpack 直接处理的 ES6 模块。在使用转译器(transpiler)时，你需要禁用对模块的处理（例如 Babel 中的 modules 选项）。</p>
</blockquote>
<h4 id="NoEmitOnErrorsPlugin"><a href="#NoEmitOnErrorsPlugin" class="headerlink" title="NoEmitOnErrorsPlugin"></a>NoEmitOnErrorsPlugin</h4><p>这个就是用于防止程序报错，就算有错误也给我继续编译，很暴力的做法呢。</p>
<h4 id="others"><a href="#others" class="headerlink" title="others"></a>others</h4><p>还有一些默认的插件配置，也就是可以不在plugins中引用的配置：</p>
<h5 id="flagIncludedChunks"><a href="#flagIncludedChunks" class="headerlink" title="flagIncludedChunks"></a>flagIncludedChunks</h5><p>flagIncludedChunks这个配置的作用是，看结果：</p>
<p>未启用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"> Asset       Size  Chunks             Chunk Names</span><br><span class="line">index.js   1.02 KiB       0  [emitted]  index</span><br><span class="line">page1.js  970 bytes       1  [emitted]  page1</span><br></pre></td></tr></table></figure>

<p>启用后，如果只有二个文件似乎表现不明显，于是我增加了三个文件，page1调用page2，index调用page1，那么一目了然，在这里的chunks就是所有引用模块的id。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"> Asset       Size   Chunks             Chunk Names</span><br><span class="line">index.js   1.08 KiB  0, 1, 2  [emitted]  index</span><br><span class="line">page1.js   1.01 KiB     1, 2  [emitted]  page1</span><br><span class="line">page2.js  971 bytes        2  [emitted]  page2</span><br></pre></td></tr></table></figure>

<h5 id="OccurrenceOrderPlugin"><a href="#OccurrenceOrderPlugin" class="headerlink" title="OccurrenceOrderPlugin"></a>OccurrenceOrderPlugin</h5><p><code>webpack.optimize.OccurrenceOrderPlugin</code>这个插件的作用是按照chunk引用次数来安排出现顺序，因为这让经常引用的模块和chunk拥有更小的id。将上面的例子加上这个配置运行下就是这样的。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">   Asset       Size   Chunks             Chunk Names</span><br><span class="line">page2.js  969 bytes        0  [emitted]  page2</span><br><span class="line">page1.js   1.01 KiB     1, 0  [emitted]  page1</span><br><span class="line">index.js   1.08 KiB  2, 0, 1  [emitted]  index</span><br></pre></td></tr></table></figure>

<h5 id="SideEffectsFlagPlugin"><a href="#SideEffectsFlagPlugin" class="headerlink" title="SideEffectsFlagPlugin"></a>SideEffectsFlagPlugin</h5><p><code>webpack.optimize.SideEffectsFlagPlugin()</code>这个插件如果需要生效的话，需要两个条件，一个是导入的模块已经标记了sideEffect，即package.json中的sideEffects这个属性为false，第二个就是当前模块引用了次无副作用的模块，而且没有使用。那么在打包的时候，就不会将这个模块打包到文件中。</p>
<p>总结</p>
<p>实际上production mode下，与官方文档相比，他的配置更加等同于如下配置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module.exports &#x3D; &#123;</span><br><span class="line">    mode:&quot;none&quot;,</span><br><span class="line">    optimization:&#123;</span><br><span class="line">        flagIncludedChunks:true,</span><br><span class="line">        minimize: true,</span><br><span class="line">    &#125;,</span><br><span class="line">    plugins: [</span><br><span class="line">        new webpack.DefinePlugin(&#123; &quot;process.env.NODE_ENV&quot;: JSON.stringify(&quot;production&quot;) &#125;),</span><br><span class="line">        new webpack.optimize.ModuleConcatenationPlugin(),</span><br><span class="line">        new webpack.NoEmitOnErrorsPlugin(),</span><br><span class="line">        new webpack.optimize.SideEffectsFlagPlugin()</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>production各插件参考文档</p>
<table>
<thead>
<tr>
<th>name</th>
<th>effect</th>
</tr>
</thead>
<tbody><tr>
<td>FlagDependencyUsagePlugin</td>
<td>标记没有用到的依赖，这个插件无法通过webpack获取，我只能通过强行导入webpack/lib下的class文件来导入。</td>
</tr>
<tr>
<td>SideEffectsFlagPlugin</td>
<td>用于处理tree shaking的,tree shaking，sideEffect这个插件的作用就是，如果当前的模块没有引用，而且package.json中的sideEffects为false，那么打包的时候就可以将此包剔除。stackoverflow上有用的答案</td>
</tr>
<tr>
<td>FlagIncludedChunksPlugin</td>
<td>给当前chunk包含的chunkid加入chunk名之中</td>
</tr>
<tr>
<td>ModuleConcatenationPlugin</td>
<td>作用域提升</td>
</tr>
<tr>
<td>NoEmitOnErrorsPlugin</td>
<td>阻止任何报错</td>
</tr>
<tr>
<td>OccurrenceOrderPlugin</td>
<td>按照调用次数来给chunks排序</td>
</tr>
<tr>
<td>UglifyJsPlugin</td>
<td>混淆压缩</td>
</tr>
</tbody></table>
<p>参考：</p>
<p><a href="https://www.cnblogs.com/cherryvenus/p/9808320.html" target="_blank" rel="noopener">显微镜下的webpack4的新特性：mode详解</a></p>
<h3 id="production-模式启用的插件"><a href="#production-模式启用的插件" class="headerlink" title="production 模式启用的插件"></a>production 模式启用的插件</h3><h4 id="FlagDependencyUsagePlugin"><a href="#FlagDependencyUsagePlugin" class="headerlink" title="FlagDependencyUsagePlugin"></a>FlagDependencyUsagePlugin</h4><ul>
<li><p>触发时机：compilation.hooks.optimizeDependencies</p>
</li>
<li><p>功能：标记模块导出中被使用的导出，存在 module.usedExports 里。用于 Tree shaking。</p>
</li>
<li><p>对应配置项：optimization.usedExports:true</p>
</li>
</ul>
<h4 id="FlagIncludedChunksPlugin"><a href="#FlagIncludedChunksPlugin" class="headerlink" title="FlagIncludedChunksPlugin"></a>FlagIncludedChunksPlugin</h4><ul>
<li><p>触发时机：compilation.hooks.optimizeChunkId</p>
</li>
<li><p>功能：给每个 chunk 添加了 ids，用于判断避免加载不必要的 chunk</p>
</li>
</ul>
<h4 id="ModuleConcatenationPlugin-1"><a href="#ModuleConcatenationPlugin-1" class="headerlink" title="ModuleConcatenationPlugin"></a>ModuleConcatenationPlugin</h4><ul>
<li><p>触发时机：compilation.hooks.optimizeChunkModules</p>
</li>
<li><p>功能：使用 esm 语法可以作用域提升(Scope Hoisting)或预编译所有模块到一个闭包中，提升代码在浏览器中的执行速度</p>
</li>
<li><p>对应配置项：optimization.concatenateModules:true</p>
</li>
</ul>
<h4 id="NoEmitOnErrorsPlugin-1"><a href="#NoEmitOnErrorsPlugin-1" class="headerlink" title="NoEmitOnErrorsPlugin"></a>NoEmitOnErrorsPlugin</h4><ul>
<li><p>触发时机：compiler.hooks.shouldEmit，compilation.hooks.shouldRecord</p>
</li>
<li><p>功能：如果在 compilation 编译时有 error，则不执行 Record 相关的钩子，并且抛错和不编译资源</p>
</li>
</ul>
<h4 id="OccurrenceOrderModuleIdsPlugin，OccurrenceOrderChunkIdsPlugin"><a href="#OccurrenceOrderModuleIdsPlugin，OccurrenceOrderChunkIdsPlugin" class="headerlink" title="OccurrenceOrderModuleIdsPlugin，OccurrenceOrderChunkIdsPlugin"></a>OccurrenceOrderModuleIdsPlugin，OccurrenceOrderChunkIdsPlugin</h4><p>注意不是文档写的 OccurrenceOrderPlugin，这个没用</p>
<ul>
<li><p>触发时机：compilation.hooks.optimizeModuleOrder，compilation.hooks.optimizeChunkOrder</p>
</li>
<li><p>功能：根据模块初始调用次数或者总调用次数排序（配置），这样在后面分配 ID 的时候常被调用 ID 就靠前，除此之外，还可以让 id 为路径，hash 等。</p>
</li>
<li><p>对应配置项：optimization.occurrenceOrder，optimization.chunkIds，optimization.moduleIds</p>
</li>
</ul>
<h4 id="SideEffectsFlagPlugin-1"><a href="#SideEffectsFlagPlugin-1" class="headerlink" title="SideEffectsFlagPlugin"></a>SideEffectsFlagPlugin</h4><ul>
<li><p>触发时机：normalModuleFactory.hooks.module，compilation.hooks.optimizeDependencies</p>
</li>
<li><p>功能：<br>normalModuleFactory.hooks.module 钩子里读取 package.json 里的 sideEffects 字段和读取 module.rule 里的 sideEffects 赋给 module.factoryMeta（纯的 ES2015 模块)；<br>compilation.hooks.optimizeDependencies 钩子里根据 sideEffects 配置，删除未用到的 export 导出</p>
</li>
<li><p>对应配置项：optimization.sideEffects:true（默认）</p>
</li>
<li><p>TerserPlugin</p>
</li>
<li><p>触发时机：template.hooks.hashForChunk，compilation.hooks.optimizeChunkAssets</p>
</li>
<li><p>功能：<br>在 template.hooks.hashForChunk 钩子即在 chunks 生成 hash 阶段会把压缩相关的信息也打入到里面<br>在 compilation.hooks.optimizeChunkAssets 钩子触发资源压缩事件</p>
</li>
<li><p>对应配置项：<br>optimization.minimize 是否开启压缩<br>optimization.minimizer 定制 Terser</p>
</li>
</ul>
<p>另：development 模式单独启用的插件：</p>
<h4 id="NamedChunksPlugin"><a href="#NamedChunksPlugin" class="headerlink" title="NamedChunksPlugin"></a>NamedChunksPlugin</h4><ul>
<li>触发时机：compilation.hooks.beforeChunkIds</li>
<li>功能：以名称固化 chunk id</li>
<li>对应配置项：optimization.chunkIds</li>
</ul>
<h4 id="NamedModulesPlugin"><a href="#NamedModulesPlugin" class="headerlink" title="NamedModulesPlugin"></a>NamedModulesPlugin</h4><ul>
<li>触发时机：compilation.hooks.beforeModuleIds</li>
<li>功能：以名称固化 module id</li>
<li>对应配置项：optimization.moduleIds</li>
</ul>
<p>参考：</p>
<p><a href="https://zhuanlan.zhihu.com/p/102632472" target="_blank" rel="noopener">webpack 4 源码主流程分析（十四）：webpack 优化</a></p>
<h3 id="Webpack-插件扫盲-–-WorkboxWebpackPlugin"><a href="#Webpack-插件扫盲-–-WorkboxWebpackPlugin" class="headerlink" title="Webpack 插件扫盲 – WorkboxWebpackPlugin"></a>Webpack 插件扫盲 – WorkboxWebpackPlugin</h3><p>需先了解service worker和PWA</p>
<p><a href="https://blog.csdn.net/qq_29722281/article/details/111873764" target="_blank" rel="noopener">service worker</a></p>
<p><a href="https://blog.csdn.net/qq_29722281/article/details/111935022" target="_blank" rel="noopener">PWA</a></p>
<h3 id="webpack压缩js代码插件"><a href="#webpack压缩js代码插件" class="headerlink" title="webpack压缩js代码插件"></a>webpack压缩js代码插件</h3><p>webpack.TerserWebpackPlugin 和</p>
<p>webpack.UglifyjsWebpackPlugin插件</p>
<p><a href="https://blog.csdn.net/qq_29722281/article/details/109489930" target="_blank" rel="noopener">https://blog.csdn.net/qq_29722281/article/details/109489930</a></p>
<h3 id="浅析js中的纯函数、高阶函数、记忆函数、偏函数"><a href="#浅析js中的纯函数、高阶函数、记忆函数、偏函数" class="headerlink" title="浅析js中的纯函数、高阶函数、记忆函数、偏函数"></a>浅析js中的纯函数、高阶函数、记忆函数、偏函数</h3>]]></content>
      <tags>
        <tag>月份记录</tag>
      </tags>
  </entry>
  <entry>
    <title>浅析js中的纯函数、高阶函数、记忆函数、偏函数</title>
    <url>/js%E5%90%84%E7%A7%8D%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>浅谈下js中的各种函数的意义与使用场景</p>
<h2 id="纯函数"><a href="#纯函数" class="headerlink" title="纯函数"></a>纯函数</h2><p>纯函数是函数式编程中非常重要的一个概念，简单来说，就是一个函数的返回结果只依赖于它的参数，并且在执行过程中没有副作用，我们就把这个函数叫做纯函数</p>
<p>定义<br>一个函数，如果符合以下两个特点，那么它就可以称之为纯函数</p>
<p>对于相同的输入，永远得到相同的输出（函数的返回结果只依赖于它的参数）</p>
<p>没有任何可观察到的副作用（函数执行过程中没有副作用）</p>
<p><strong>解析–函数的返回结果只依赖于它的参数</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let greeting&#x3D; &#39;Hello&#39;</span><br><span class="line"> </span><br><span class="line">function greet (name) &#123;</span><br><span class="line">  return greeting+ &#39; &#39; + name</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">console.log(greet(&#39;World&#39;)) &#x2F;&#x2F; Hello World</span><br></pre></td></tr></table></figure>

<p>上面代码中，greet函数不是一个纯函数，因为它的返回结果依赖外部变量greeting，因为greeting是有可能变化的，所以我们不能保证<code>greet(&#39;World&#39;)</code>的值永远是<code>Hello World</code>。虽然greet函数的代码没有变化，传入的参数也没有变化，但它的返回值是不可预料的</p>
<p>经过以下调增后</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function greet (greeting, name) &#123;</span><br><span class="line">  return greeting +&#39; &#39; + name</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">console.log(greet(&#39;Hi&#39;,&#39;Savo&#39;))  &#x2F;&#x2F; Hi Savo</span><br></pre></td></tr></table></figure>

<p>现在，greet的返回结果只依赖于它的参数greeting和name，就是说，只要代码不变，<code>greet(&#39;Hi&#39;, &#39;Savo&#39;)</code>的返回值永远是<code>Hi Savo</code></p>
<p>这就是纯函数的第一个条件：函数的返回结果只依赖于它的参数</p>
<p><strong>解析–没有副作用</strong></p>
<p>没有副作用的意思是，这个函数的运行，不会修改外部的状态，即不会污染外部作用域</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const user &#x3D; &#123;</span><br><span class="line">  name:&#39;Nick Brown&#39;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">let flag &#x3D;false</span><br><span class="line"> </span><br><span class="line">function checkData (user) &#123;</span><br><span class="line">  if (user.name.length &gt; 4) &#123;</span><br><span class="line">    flag &#x3D;true</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可见，执行函数的时候会修改到 flag 的值（注意：如果函数没有任何返回值，那么它很可能就具有副作用）</p>
<p>由于定义的函数改变的值在函数作用域之外，导致这个函数成为“不纯”的函数</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const user &#x3D; &#123;</span><br><span class="line">  name:&#39;Nick Brown&#39;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">function checkData (user) &#123;</span><br><span class="line">  return user.name.length &gt; 4</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">const flag &#x3D; checkData(user)</span><br></pre></td></tr></table></figure>

<p>上面的代码，我们只计算了作用域内的局部变量，没有任何作用域外部的变量被改变，因此这个函数是“纯函数”</p>
<p>除了修改外部的变量，一个函数在执行过程中还有很多方式产生外部可观察的变化，比如说调用 DOM API 修改页面，或者你发送了 Ajax 请求，还有调用<code>window.reload</code>刷新浏览器，甚至是<code>console.log</code>往控制台打印数据也是副作用</p>
<p>纯函数很严格，也就是说你几乎除了计算数据以外什么都不能干，计算的时候还不能依赖除了函数参数以外的数据</p>
<p>特殊例子</p>
<p>再来看看JavaScript中常用的两个方法slice和splice</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const arr1 &#x3D; [0, 1, 2, 3, 4, 5, 6]</span><br><span class="line">const arr2 &#x3D; [0, 1, 2, 3, 4, 5 ,6]</span><br><span class="line"> </span><br><span class="line">const spliceArr &#x3D; arr1.splice(0, 2)</span><br><span class="line">const sliceArr &#x3D; arr2.slice(0, 2)</span><br><span class="line"> </span><br><span class="line">console.log(&#39;arr1: &#39;, arr1)</span><br><span class="line">console.log(&#39;spliceArray: &#39;, spliceArr)</span><br><span class="line"> </span><br><span class="line">console.log(&#39;arr2: &#39;, arr2)</span><br><span class="line">console.log(&#39;sliceArray: &#39;, sliceArr)</span><br><span class="line"> </span><br><span class="line">&#x2F;&#x2F; 执行结果</span><br><span class="line">array1: 2,3,4,5,6</span><br><span class="line">spliceArray: 0,1</span><br><span class="line">array2: 0,1,2,3,4,5,6</span><br><span class="line">sliceArray: 0,1</span><br></pre></td></tr></table></figure>

<p>可以看到，slice和splice的作用是大致相同的，但是splice改变了原数组，而slice却没有，实际开发中，slice这种不改变原数组的方式更安全一些，改变原始数组，是一种副作用</p>
<p>场景</p>
<p>纯函数可以根据输入来做缓存，实现缓存的是一种叫做<code>memorize</code>的技术，例Vue的源码</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;**</span><br><span class="line"> * Create a cached version of a pure function.</span><br><span class="line"> * 只适用于缓存 接收一个字符串为参数的 fn</span><br><span class="line"> *&#x2F;</span><br><span class="line">export function cached (fn) &#123;</span><br><span class="line">  const cache &#x3D; Object.create(null)</span><br><span class="line">  return function cachedFn (str) &#123;</span><br><span class="line">    const hit &#x3D; cache[str]</span><br><span class="line">    return hit || (cache[str] &#x3D; fn(str))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">&#x2F;**</span><br><span class="line"> * Capitalize a string.</span><br><span class="line"> *&#x2F;</span><br><span class="line">export const capitalize &#x3D; cached((str) &#x3D;&gt; &#123;</span><br><span class="line">  return str.charAt(0).toUpperCase() + str.slice(1)</span><br></pre></td></tr></table></figure>

<p>优点</p>
<ul>
<li>可复用性/可移植性</li>
</ul>
<p>纯函数仅依赖于传入的参数，这意味着你可以随意将这个函数移植到别的代码中，只需要提供踏需要的参数即可。如果是非纯函数，有可能你需要一根香蕉，却需要将整个香蕉树搬过去(开发中最好是纯函数)</p>
<ul>
<li>可测试性</li>
</ul>
<p>纯函数非常容易进行单元测试，因为不需要考虑上下文环境，只需要考虑输入和输出</p>
<ul>
<li>并行代码</li>
</ul>
<p>纯函数是健壮的，改变执行次序不会对系统造成影响，因此纯函数的操作可以并行执行</p>
<h2 id="高阶函数"><a href="#高阶函数" class="headerlink" title="高阶函数"></a>高阶函数</h2><p><a href="/js高阶函数" target="_blank">点击这里</a></p>
<h2 id="记忆函数"><a href="#记忆函数" class="headerlink" title="记忆函数"></a>记忆函数</h2><p>简介</p>
<p>记忆化（memoization）是一种构建函数的处理过程，能够记住上次计算结果的函数。在实现中，可以这样进行处理：当函数计算得到结果时，就将该结果按照参数存储起来。采取这种方式时，如果另外一个调用也使用相同的参数，我们则可以直接返回上次存储的结果而不是再计算一遍</p>
<p>显而易见，像这样避免既重复又复杂的计算可以显著提高性能。对于动画中的计算、搜索不经常变化的数据或任何耗时的数学计算来说，记忆化这种方式是十分有用的</p>
<p>实例分析</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 自记忆素数检测函数</span><br><span class="line">function isPrime (value) &#123;</span><br><span class="line">  &#x2F;&#x2F; 创建缓存</span><br><span class="line">  if (!isPrime.answers) &#123;</span><br><span class="line">    isPrime.answers &#x3D; &#123;&#125;;</span><br><span class="line">  &#125;</span><br><span class="line">  &#x2F;&#x2F; 检查缓存的值</span><br><span class="line">  if (isPrime.answers[value] !&#x3D;&#x3D; undefined) &#123;</span><br><span class="line">    return isPrime.answers[value];</span><br><span class="line">  &#125;</span><br><span class="line">  &#x2F;&#x2F; 0和1不是素数</span><br><span class="line">  var prime &#x3D; value !&#x3D;&#x3D; 0 &amp;&amp; value !&#x3D;&#x3D; 1;</span><br><span class="line">  &#x2F;&#x2F; 检查是否为素数</span><br><span class="line">  for (var i &#x3D; 2; i &lt; value; i++) &#123;</span><br><span class="line">    if (value % i &#x3D;&#x3D;&#x3D; 0) &#123;</span><br><span class="line">      prime &#x3D;false;</span><br><span class="line">      break;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  &#x2F;&#x2F; 存储计算值</span><br><span class="line">  return isPrime.answers[value] &#x3D; prime</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>isPrime函数是一个自记忆素数检测函数，每当它被调用时</p>
<ul>
<li>首先，检查它的answers属性来确认是否已经有自记忆的缓存，如果没有，创建一个</li>
<li>接下来，检查参数之前是否已经被缓存过，如果在缓存中找到该值，直接返回缓存的结果</li>
<li>如果参数是一个全新的值，进行正常的素数检测</li>
<li>最后，存储并返回计算值</li>
</ul>
<p>优缺点<br>优点</p>
<ul>
<li>由于函数调用时会寻找之前调用所得到的值，所以用户最终会乐于看到所获得的性能收益</li>
<li>它不需要执行任何特殊请求，也不需要做任何额外初始化，就能顺利进行工作</li>
</ul>
<p>缺陷</p>
<ul>
<li>任何类型的缓存都必然会为性能牺牲内存</li>
<li>缓存逻辑不应该和业务逻辑混合，一个方法只需要把一件事情做好</li>
<li>对记忆函数很难做负载测试或估算算法复杂度，因为结果依赖于函数之前的输入</li>
</ul>
<h2 id="偏函数"><a href="#偏函数" class="headerlink" title="偏函数"></a>偏函数</h2><p>简介</p>
<p>偏函数属于函数式编程的一部分，使用偏函数可以通过有效地“冻结”那些预先确定的参数，来缓存函数参数，然后在运行时，当获得需要的剩余参数后，可以将他们解冻，传递到最终的参数中，从而使用最终确定的所有参数去调用函数</p>
<p>简而言之，偏函数是<strong>JS 函数柯里化</strong>运算的一种特定应用场景，就是把一个函数的某些参数先固化，也就是设置默认值，返回一个新的函数，在新函数中继续接收剩余参数，这样调用这个新函数会更简单</p>
<p>常见场景试例<br>将一些函数组合封装到一个函数中，调用时可以按顺序实现全部功能</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function toUpperCase(str) &#123;</span><br><span class="line">    return str.toUpperCase()&#x2F;&#x2F; 将字符串变成大写</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">function add(str) &#123;</span><br><span class="line">    return str +&#39;!!!&#39; &#x2F;&#x2F; 将字符串拼接</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">function split(str) &#123;</span><br><span class="line">    return str.split(&#39;&#39;)&#x2F;&#x2F; 将字符串拆分为数组</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">function reverse(arr) &#123;</span><br><span class="line">    return arr.reverse()&#x2F;&#x2F; 将数组逆序</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">function join(arr) &#123;</span><br><span class="line">    return arr.join(&#39;-&#39;)&#x2F;&#x2F; 将数组按&#39;-&#39;拼接成字符串</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">function compose() &#123;</span><br><span class="line">    const args &#x3D; Array.prototype.slice.call(arguments) &#x2F;&#x2F; 类数组转换为数组</span><br><span class="line">    const len &#x3D; args.length - 1 &#x2F;&#x2F; 最后一个参数的索引</span><br><span class="line">    return function(x) &#123;</span><br><span class="line">        let result &#x3D; args[len](x) &#x2F;&#x2F; 执行最后一个函数的结果</span><br><span class="line">        while(len--) &#123;</span><br><span class="line">            result &#x3D; args[len](result) &#x2F;&#x2F; 执行每个函数的结果</span><br><span class="line">        &#125;</span><br><span class="line">        return result</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">const f &#x3D; compose(add, join, reverse, split, toUpperCase)</span><br><span class="line">console.log( f(&#39;cba&#39;) ) &#x2F;&#x2F; A-B-C!!!</span><br></pre></td></tr></table></figure>

<p>在组合函数 compose 中，依次执行 toUpperCase、split、reverse、join、add 实现全部功能</p>
<p>当然有更优雅的写法，通过数组自带的方法实现</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const compose2 &#x3D; (...args) &#x3D;&gt; x &#x3D;&gt; args.reduceRight((result, cb) &#x3D;&gt; cb(res), x)</span><br><span class="line"> </span><br><span class="line">const f &#x3D; compose2(add, join, reverse, split, toUpperCase)</span><br><span class="line">console.log( f(&#39;cba&#39;) )  &#x2F;&#x2F; A-B-C!!!</span><br></pre></td></tr></table></figure>

<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://www.cnblogs.com/zhazhanitian/p/13063999.html" target="_blank" rel="noopener">浅析js中的纯函数、高阶函数、记忆函数、偏函数
</a></p>
]]></content>
  </entry>
  <entry>
    <title>js高阶函数</title>
    <url>/js%E9%AB%98%E9%98%B6%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>高阶函数是那些将其他函数作为参数或返回其他函数的函数。在高阶函数中作为参数传递的函数被称为回调。</p>
<p>特点</p>
<ul>
<li>函数可以作为参数传递（接受一个或多个函数作为输入）</li>
<li>函数可以作为返回值输出（输出一个函数）</li>
</ul>
<p>应用场景：</p>
<h3 id="1-JS的回调函数-常用"><a href="#1-JS的回调函数-常用" class="headerlink" title="1. JS的回调函数(常用)"></a>1. JS的回调函数(常用)</h3><p>例如：<code>map()</code>、<code>filter()</code>、<code>reduce</code>、<code>reduceRight()</code>、<code>sort()</code></p>
<h3 id="2-使用闭包相关的场景-常用"><a href="#2-使用闭包相关的场景-常用" class="headerlink" title="2. 使用闭包相关的场景(常用)"></a>2. 使用闭包相关的场景(常用)</h3><p>例如：防抖函数（debounce）与节流函数（throttle）</p>
<ul>
<li>防抖函数：在多个事件触发后200ms才执行，实现：当事件触发时当盼到有定时器时清楚定时器</li>
<li>节流函数：多个事件间隔200ms执行一次，实现：当事件触发时判断有定时器就跳过。</li>
</ul>
<h3 id="3-JS单例模式"><a href="#3-JS单例模式" class="headerlink" title="3. JS单例模式"></a>3. JS单例模式</h3><p>单例就是保证一个类只有一个实例。实现方法：先判断实例是否存在，如果存在则直接返回，否则就创建实例再返回。</p>
<p>单例模式可以确保一个类型只有一个实例对象。在 JavaScript 中，单例可以作为一个命名空间，提供一个唯一的访问点来访问该对象。单例模式封装代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var getSingle &#x3D; function (fn) &#123;</span><br><span class="line">    var ret;</span><br><span class="line">    return function () &#123;</span><br><span class="line">        return ret || (ret &#x3D; fn.apply(this, arguments));</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="4-JS实现-AOP"><a href="#4-JS实现-AOP" class="headerlink" title="4. JS实现 AOP"></a>4. JS实现 AOP</h3><p>AOP（面向切面编程）就是把一些与业务逻辑模块无关的功能抽离出来，如日志统计、安全控制、异常处理等，然后通过“动态织入”的方式掺入业务逻辑模块中。这样设计的好处是：首先可以保证业务逻辑模块的纯净和高内聚性；其次可以方便地复用日志统计等功能模块。<br>示例</p>
<p>在 JavaScript 中实现 AOP，一般是把一个函数“动态织入”到另外一个函数中。具体的实现方法有很多，下面通过扩展 Function.prototype 方法实现 AOP。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Function.prototype.before &#x3D; function (beforefn) &#123;</span><br><span class="line">    var __self &#x3D; this;  &#x2F;&#x2F;保存原函数的引用</span><br><span class="line">    return function () &#123;  &#x2F;&#x2F;返回包含了原函数和新函数的“代理”函数</span><br><span class="line">        beforefn.apply(this, arguments);  &#x2F;&#x2F;执行新函数</span><br><span class="line">        return __self.apply(this, arguments);  &#x2F;&#x2F;执行原函数</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">Function.prototype.after &#x3D; function (afterfn) &#123;</span><br><span class="line">    var __self &#x3D; this;  &#x2F;&#x2F;保存原函数的引用</span><br><span class="line">    return function () &#123;  &#x2F;&#x2F;返回包含了原函数和新函数的“代理”函数</span><br><span class="line">       var ret &#x3D; __self.apply(this,arguments);  &#x2F;&#x2F;执行原函数</span><br><span class="line">        afterfn.apply(this, arguments);  &#x2F;&#x2F;执行新函数，修正this</span><br><span class="line">        return ret;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">var func &#x3D; function ()&#123;</span><br><span class="line">    console.log(2);</span><br><span class="line">&#125;;</span><br><span class="line">func &#x3D; func.before(function () &#123;</span><br><span class="line">    console.log(1);</span><br><span class="line">&#125;).after(function () &#123;</span><br><span class="line">    console.log(3)</span><br><span class="line">&#125;);</span><br><span class="line">func();  &#x2F;&#x2F;按顺序输出1,2,3</span><br></pre></td></tr></table></figure>

<h3 id="5-JS-类型检测"><a href="#5-JS-类型检测" class="headerlink" title="5. JS 类型检测"></a>5. JS 类型检测</h3><p>利用 JavaScript 高阶函数特性来重新设计 typeOf() 函数，并提供单项类型判断函数。</p>
<p>实现代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function typeOf(obj) &#123;  &#x2F;&#x2F;类型检测函数，返回字符串表示</span><br><span class="line">    var str &#x3D; Object.prototype.toString.call(obj);</span><br><span class="line">    return str.match(&#x2F;\[object(.*?)\]&#x2F;)[1].toLowerCase();</span><br><span class="line">&#125;;</span><br><span class="line">[&#39;null&#39;, &#39;Undefined&#39;, &#39;Object&#39;, &#39;Array&#39;, &#39;String&#39;, &#39;Number&#39;, &#39;Boolean&#39;, &#39;Function&#39;, &#39;RegExp&#39;].forEach(function (t) &#123;  &#x2F;&#x2F;类型判断，返回布尔值</span><br><span class="line">    typeOf[&#39;is&#39; + t] &#x3D; function (o) &#123;</span><br><span class="line">        return typeOf(o) &#x3D;&#x3D;&#x3D; t.toLowerCase();</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>应用代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;类型检测</span><br><span class="line">console.log(typeOf(&#123;&#125;));  &#x2F;&#x2F;&quot;object&quot;</span><br><span class="line">console.log(typeOf([]));  &#x2F;&#x2F;&quot;array&quot;</span><br><span class="line">console.log(typeOf(0));  &#x2F;&#x2F;&quot;number&quot;</span><br><span class="line">console.log(typeOf(null));  &#x2F;&#x2F;&quot;null&quot;</span><br><span class="line">console.log(typeOf(undefined));  &#x2F;&#x2F;&quot;undefined&quot;</span><br><span class="line">console.log(typeOf(&#x2F;&#x2F;));  &#x2F;&#x2F;&quot;regex&quot;</span><br><span class="line">console.log(typeOf(new Date()));  &#x2F;&#x2F;&quot;date&quot;</span><br><span class="line">&#x2F;&#x2F;类型判断</span><br><span class="line">console.log(typeOf.isObject(&#123;&#125;));  &#x2F;&#x2F;true</span><br><span class="line">console.log(typeOf.isNumber(NaN));  &#x2F;&#x2F;true</span><br><span class="line">console.log(typeOf.isRegExp(true));  &#x2F;&#x2F;false</span><br></pre></td></tr></table></figure>

<h3 id="6-尾调用和尾递归"><a href="#6-尾调用和尾递归" class="headerlink" title="6. 尾调用和尾递归"></a>6. 尾调用和尾递归</h3><p>尾调用（Tail Call）是函数式编程的一个重要概念，本身非常简单，一句话就能说清楚。就是指某个函数的最后一步是调用另一个函数。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function g(x) &#123;</span><br><span class="line">  console.log(x)</span><br><span class="line">&#125;</span><br><span class="line">function f(x) &#123;</span><br><span class="line">  return g(x)</span><br><span class="line">&#125;</span><br><span class="line">console.log(f(1))</span><br><span class="line">&#x2F;&#x2F;上面代码中，函数f的最后一步是调用函数g，这就是尾调用。</span><br></pre></td></tr></table></figure>

<p>上面代码中，函数 f 的最后一步是调用函数 g，这就是尾调用。尾调用不一定出现在函数尾部，只要是最后一步操作即可。</p>
<p>函数调用自身，称为递归。如果尾调用自身，就称为尾递归。递归非常耗费内存，因为需要同时保存成千上百个调用帧，很容易发生栈溢出错误。但是队伍尾递归来说，由于只存在一个调用帧，所以永远不会发生栈溢出错误。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function factorial(n) &#123;</span><br><span class="line">  if (n &#x3D;&#x3D;&#x3D; 1) &#123;</span><br><span class="line">    return 1</span><br><span class="line">  &#125;</span><br><span class="line">  return n * factorial(n - 1)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码是一个阶乘函数，计算 n 的阶乘，最多需要保存 n 个调用数据，复杂度为 O（n），如果改写成尾调用，只保留一个调用记录，复杂度为 O（1）。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function factor(n, total) &#123;</span><br><span class="line">  if (n &#x3D;&#x3D;&#x3D; 1) &#123;</span><br><span class="line">    return total</span><br><span class="line">  &#125;</span><br><span class="line">  return factor(n - 1, n * total)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>斐波拉切数列也是可以用于尾调用。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function Fibonacci(n) &#123;</span><br><span class="line">  if (n &lt;&#x3D; 1) &#123;</span><br><span class="line">    return 1</span><br><span class="line">  &#125;</span><br><span class="line">  return Fibonacci(n - 1) + Fibonacci(n - 2)</span><br><span class="line">&#125;</span><br><span class="line">&#x2F;&#x2F;尾递归</span><br><span class="line">function Fibona(n, ac1 &#x3D; 1, ac2 &#x3D; 1) &#123;</span><br><span class="line">  if (n &lt;&#x3D; 1) &#123;</span><br><span class="line">    return ac2</span><br><span class="line">  &#125;</span><br><span class="line">  return Fibona(n - 1, ac2, ac1 + ac2)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="柯理化函数"><a href="#柯理化函数" class="headerlink" title="柯理化函数"></a>柯理化函数</h3><p>在数学和计算机科学中，柯里化是一种将使用多个参数的一个函数转换成一系列使用一个参数的函数的技术。<strong>所谓柯里化就是把具有较多参数的函数转换成具有较少参数的函数的过程。</strong><br>举个例子</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;普通函数</span><br><span class="line">function fn(a, b, c, d, e) &#123;</span><br><span class="line">  console.log(a, b, c, d, e)</span><br><span class="line">&#125;</span><br><span class="line">&#x2F;&#x2F;生成的柯里化函数</span><br><span class="line">let _fn &#x3D; curry(fn)</span><br><span class="line"></span><br><span class="line">_fn(1, 2, 3, 4, 5) &#x2F;&#x2F; print: 1,2,3,4,5</span><br><span class="line">_fn(1)(2)(3, 4, 5) &#x2F;&#x2F; print: 1,2,3,4,5</span><br><span class="line">_fn(1, 2)(3, 4)(5) &#x2F;&#x2F; print: 1,2,3,4,5</span><br><span class="line">_fn(1)(2)(3)(4)(5) &#x2F;&#x2F; print: 1,2,3,4,5</span><br></pre></td></tr></table></figure>

<p>柯理化函数的实现</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 对求和函数做curry化</span><br><span class="line">let f1 &#x3D; curry(add, 1, 2, 3)</span><br><span class="line">console.log(&#39;复杂版&#39;, f1()) &#x2F;&#x2F; 6</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 对求和函数做curry化</span><br><span class="line">let f2 &#x3D; curry(add, 1, 2)</span><br><span class="line">console.log(&#39;复杂版&#39;, f2(3)) &#x2F;&#x2F; 6</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 对求和函数做curry化</span><br><span class="line">let f3 &#x3D; curry(add)</span><br><span class="line">console.log(&#39;复杂版&#39;, f3(1, 2, 3)) &#x2F;&#x2F; 6</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 复杂版curry函数可以多次调用，如下：</span><br><span class="line">console.log(&#39;复杂版&#39;, f3(1)(2)(3)) &#x2F;&#x2F; 6</span><br><span class="line">console.log(&#39;复杂版&#39;, f3(1, 2)(3)) &#x2F;&#x2F; 6</span><br><span class="line">console.log(&#39;复杂版&#39;, f3(1)(2, 3)) &#x2F;&#x2F; 6</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 复杂版(每次可传入不定数量的参数，当所传参数总数不少于函数的形参总数时，才会执行)</span><br><span class="line">function curry(fn) &#123;</span><br><span class="line">  &#x2F;&#x2F; 闭包</span><br><span class="line">  &#x2F;&#x2F; 缓存除函数fn之外的所有参数</span><br><span class="line">  let args &#x3D; Array.prototype.slice.call(arguments, 1)</span><br><span class="line">  return function() &#123;</span><br><span class="line">    &#x2F;&#x2F; 连接已缓存的老的参数和新传入的参数(即把每次传入的参数全部先保存下来，但是并不执行)</span><br><span class="line">    let newArgs &#x3D; args.concat(Array.from(arguments))</span><br><span class="line">    if (newArgs.length &lt; fn.length) &#123;</span><br><span class="line">      &#x2F;&#x2F; 累积的参数总数少于fn形参总数</span><br><span class="line">      &#x2F;&#x2F; 递归传入fn和已累积的参数</span><br><span class="line">      return curry.call(this, fn, ...newArgs)</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      &#x2F;&#x2F; 调用</span><br><span class="line">      return fn.apply(this, newArgs)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="柯里化的用途"><a href="#柯里化的用途" class="headerlink" title="柯里化的用途"></a>柯里化的用途</h4><p>柯里化实际是把简答的问题复杂化了，但是复杂化的同时，我们在使用函数时拥有了更加多的自由度。 而这里对于函数参数的自由处理，正是柯里化的核心所在。 柯里化本质上是降低通用性，提高适用性。来看一个例子：</p>
<p>我们工作中会遇到各种需要通过正则检验的需求，比如校验电话号码、校验邮箱、校验身份证号、校验密码等， 这时我们会封装一个通用函数 checkByRegExp ,接收两个参数，校验的正则对象和待校验的字符串</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function checkByRegExp(regExp, string) &#123;</span><br><span class="line">  return regExp.text(string)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">checkByRegExp(&#x2F;^1\d&#123;10&#125;$&#x2F;, &#39;18642838455&#39;) &#x2F;&#x2F; 校验电话号码</span><br><span class="line">checkByRegExp(&#x2F;^(\w)+(\.\w+)*@(\w)+((\.\w+)+)$&#x2F;, &#39;test@163.com&#39;) &#x2F;&#x2F; 校验邮箱</span><br></pre></td></tr></table></figure>

<p>我们每次进行校验的时候都需要输入一串正则，再校验同一类型的数据时，相同的正则我们需要写多次， 这就导致我们在使用的时候效率低下，并且由于 checkByRegExp 函数本身是一个工具函数并没有任何意义。此时，我们可以借助柯里化对 checkByRegExp 函数进行封装，以简化代码书写，提高代码可读性。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;进行柯里化</span><br><span class="line">let _check &#x3D; curry(checkByRegExp)</span><br><span class="line">&#x2F;&#x2F;生成工具函数，验证电话号码</span><br><span class="line">let checkCellPhone &#x3D; _check(&#x2F;^1\d&#123;10&#125;$&#x2F;)</span><br><span class="line">&#x2F;&#x2F;生成工具函数，验证邮箱</span><br><span class="line">let checkEmail &#x3D; _check(&#x2F;^(\w)+(\.\w+)*@(\w)+((\.\w+)+)$&#x2F;)</span><br><span class="line"></span><br><span class="line">checkCellPhone(&#39;18642838455&#39;) &#x2F;&#x2F; 校验电话号码</span><br><span class="line">checkCellPhone(&#39;13109840560&#39;) &#x2F;&#x2F; 校验电话号码</span><br><span class="line">checkCellPhone(&#39;13204061212&#39;) &#x2F;&#x2F; 校验电话号码</span><br><span class="line"></span><br><span class="line">checkEmail(&#39;test@163.com&#39;) &#x2F;&#x2F; 校验邮箱</span><br><span class="line">checkEmail(&#39;test@qq.com&#39;) &#x2F;&#x2F; 校验邮箱</span><br><span class="line">checkEmail(&#39;test@gmail.com&#39;) &#x2F;&#x2F; 校验邮箱</span><br></pre></td></tr></table></figure>

<h4 id="柯里化函数参数-length"><a href="#柯里化函数参数-length" class="headerlink" title="柯里化函数参数 length"></a>柯里化函数参数 length</h4><p>函数 currying 的实现中，使用了 fn.length 来表示函数参数的个数，那 fn.length 表示函数的所有参数个数吗？并不是。<br>函数的 length 属性获取的是形参的个数，但是形参的数量不包括剩余参数个数，而且仅包括第一个具有默认值之前的参数个数，看下面的例子。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">((a, b, c) &#x3D;&gt; &#123;&#125;).length</span><br><span class="line">&#x2F;&#x2F; 3</span><br><span class="line"></span><br><span class="line">((a, b, c &#x3D; 3) &#x3D;&gt; &#123;&#125;).length</span><br><span class="line">&#x2F;&#x2F; 2</span><br><span class="line"></span><br><span class="line">((a, b &#x3D; 2, c) &#x3D;&gt; &#123;&#125;).length</span><br><span class="line">&#x2F;&#x2F; 1</span><br><span class="line"></span><br><span class="line">((a &#x3D; 1, b, c) &#x3D;&gt; &#123;&#125;).length</span><br><span class="line">&#x2F;&#x2F; 0</span><br><span class="line"></span><br><span class="line">((...args) &#x3D;&gt; &#123;&#125;).length</span><br><span class="line">&#x2F;&#x2F; 0</span><br><span class="line"></span><br><span class="line">const fn &#x3D; (...args) &#x3D;&gt; &#123;</span><br><span class="line">  console.log(args.length)</span><br><span class="line">&#125;</span><br><span class="line">fn(1, 2, 3)</span><br><span class="line">&#x2F;&#x2F; 3</span><br></pre></td></tr></table></figure>

<h3 id="compose-函数"><a href="#compose-函数" class="headerlink" title="compose 函数"></a>compose 函数</h3><p>compose 就是组合函数，将子函数串联起来执行，一个函数的输出结果是另一个函数的输入参数，一旦第一个函数开始执行，会像多米诺骨牌一样推导执行后续函数。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const greeting &#x3D; name &#x3D;&gt; &#96;Hello $&#123;name&#125;&#96;</span><br><span class="line">const toUpper &#x3D; str &#x3D;&gt; str.toUpperCase()</span><br><span class="line"></span><br><span class="line">toUpper(greeting(&#39;Onion&#39;)) &#x2F;&#x2F; HELLO ONION</span><br></pre></td></tr></table></figure>

<p>compose 函数的特点</p>
<ul>
<li>compose 接受函数作为参数，从右向左执行，返回类型函数</li>
<li>fn()全部参数传给最右边的函数，得到结果后传给倒数第二个，依次传递</li>
</ul>
<p>compose 的实现</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var compose &#x3D; function(...args) &#123;</span><br><span class="line">  var len &#x3D; args.length &#x2F;&#x2F; args函数的个数</span><br><span class="line">  var count &#x3D; len - 1</span><br><span class="line">  var result</span><br><span class="line">  return function func(...args1) &#123;</span><br><span class="line">    &#x2F;&#x2F; func函数的args1参数枚举</span><br><span class="line">    result &#x3D; args[count].call(this, args1)</span><br><span class="line">    if (count &gt; 0) &#123;</span><br><span class="line">      count--</span><br><span class="line">      return func.call(null, result) &#x2F;&#x2F; result 上一个函数的返回结果</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      &#x2F;&#x2F;回复count初始状态</span><br><span class="line">      count &#x3D; len - 1</span><br><span class="line">      return result</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>举个例子</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var greeting &#x3D; (name) &#x3D;&gt;  &#96;Hello $&#123;name&#125;&#96;</span><br><span class="line">var toUpper &#x3D; str &#x3D;&gt; str.toUpperCase()</span><br><span class="line">var fn &#x3D; compose(toUpper, greeting)</span><br><span class="line">console.log(fn(&#39;jack&#39;))</span><br></pre></td></tr></table></figure>

<h3 id="JS-惰性载入函数与分支函数"><a href="#JS-惰性载入函数与分支函数" class="headerlink" title="JS 惰性载入函数与分支函数"></a>JS 惰性载入函数与分支函数</h3><h3 id="JS-偏函数"><a href="#JS-偏函数" class="headerlink" title="JS 偏函数"></a>JS 偏函数</h3><h3 id="JS-泛型函数"><a href="#JS-泛型函数" class="headerlink" title="JS 泛型函数"></a>JS 泛型函数</h3><p>参考：</p>
<p><a href="http://c.biancheng.net/view/5751.html" target="_blank" rel="noopener">JS高阶函数精讲</a></p>
<p><a href="https://juejin.cn/post/6844904127948914701#heading-6" target="_blank" rel="noopener">你也许不知道的javascript高级函数</a></p>
<h2 id="Q：高阶函数和普通函数的性能区别？"><a href="#Q：高阶函数和普通函数的性能区别？" class="headerlink" title="Q：高阶函数和普通函数的性能区别？"></a>Q：高阶函数和普通函数的性能区别？</h2><ol>
<li>从函数性能来看，普通函数性能更好</li>
</ol>
<p>如for循环和forEach性能区别</p>
<ol start="2">
<li>从功能上来看，高阶函数更好</li>
</ol>
<p>从节流、防抖函数来说</p>
<p><a href="https://blog.csdn.net/haochuan9421/article/details/81414532" target="_blank" rel="noopener">JS数组循环的性能和效率分析（for、while、forEach、map、for of）</a></p>
]]></content>
  </entry>
</search>
